<Window x:Class="Reflection.Shell.MainShell"
        x:Name="ShellWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:chronos="http://Reflection/schemas/2010/xaml"
        xmlns:Reflection="http://Reflection/schemas/2010/xaml"
        xmlns:res="clr-namespace:Reflection.Presentation.Resources;assembly=Reflection.Presentation.Resources"
        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
        ResizeMode="NoResize" ShowActivated="True" WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        FocusVisualStyle="{x:Null}"
        Title="Reflection"
        Icon="/Images/Reflection.png" 
        mc:Ignorable="d" WindowState="{Binding WindowState}"
        DataContext="{Binding Main, Source={StaticResource Locator}}">

    <Window.Resources>
        <!--xmlns:UIV="clr-namespace:Reflection.Modules.FICO.Views;assembly=Reflection.Modules.FICO.Views"-->
        <!--<DataTemplate DataType="{x:Type UIVM:FICO_T002_VM_POS}">
            <UIV:FICO_U0001 ts_code_vm="FI21" />
        </DataTemplate>-->
        <!--// Toggle Button Style Template //-->

        <ImageBrush x:Key="tb_mouse_over"
                    ImageSource="{StaticResource imgMenu2}"/>

        <!--// Text font style //-->

        <Style x:Key="font_style" TargetType="TextBlock">

            <Setter Property="FontFamily"
                    Value="Fonts/Roboto-Light.ttf #Roboto"/>

            <Setter Property="VerticalAlignment"
                    Value="Center"/>

            <Setter Property="HorizontalAlignment"
                    Value="Left"/>

            <Setter Property="Foreground"
                    Value="White"/>

            <Setter Property="FontSize"
                    Value="20"/>

        </Style>

        <!--// Tooltip Style Template //-->

        <Style TargetType="ToolTip" x:Key="tt_style">

            <Setter Property="Placement" Value="Right"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="Width" Value="100"/>
            <Setter Property="Margin" Value="20,0,0,0"/>
            <Setter Property="Background" Value="#4A5668"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontFamily" Value="Fonts/Roboto-Light.ttf #Roboto"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="PlacementRectangle" Value="60,0,0,0"/>
            <Setter Property="HorizontalOffset" Value="15"/>
            <Setter Property="VerticalOffset" Value="6"/>
            <Setter Property="HasDropShadow" Value="True"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>

        </Style>

        <!--// App Close Button Style Template //-->

        <ImageBrush x:Key="CloseBtn_MouseOver" ImageSource="{StaticResource imgMenu2}"/>

        <Style x:Key="Close_btn" TargetType="{x:Type Button}">

            <Setter Property="Template">

                <Setter.Value>

                    <ControlTemplate TargetType="{x:Type Button}">

                        <Border x:Name="button"
                                BorderThickness="0"
                                Background="{TemplateBinding Background}"
                                SnapsToDevicePixels="True">

                        </Border>

                        <ControlTemplate.Triggers>

                            <Trigger Property="IsMouseOver"
                                     Value="True">

                                <Setter Property="Background"
                                        TargetName="button"
                                        Value="{StaticResource CloseBtn_MouseOver}"/>

                            </Trigger>

                        </ControlTemplate.Triggers>

                    </ControlTemplate>

                </Setter.Value>

            </Setter>

        </Style>

        <Style x:Key="tb_style" TargetType="{x:Type ToggleButton}">

            <Setter Property="Template">

                <Setter.Value>

                    <ControlTemplate TargetType="{x:Type ToggleButton}">

                        <Border  x:Name="border"
                                 BorderThickness="{TemplateBinding BorderThickness}"
                                 Background="{TemplateBinding Background}"
                                 SnapsToDevicePixels="True">

                        </Border>

                        <ControlTemplate.Triggers>

                            <Trigger Property="IsMouseOver" Value="true">

                                <Setter Property="Background"
                                        TargetName="border"
                                        Value="{StaticResource tb_mouse_over}"/>

                                <Setter Property="BorderBrush"
                                        TargetName="border"
                                        Value="{StaticResource tb_mouse_over}"/>

                            </Trigger>

                            <Trigger Property="IsChecked" Value="true">

                                <Setter Property="Background"
                                        TargetName="border"
                                        Value="{StaticResource tb_mouse_over}"/>

                                <Setter Property="BorderBrush"
                                        TargetName="border"
                                        Value="{StaticResource tb_mouse_over}"/>

                            </Trigger>

                        </ControlTemplate.Triggers>

                    </ControlTemplate>

                </Setter.Value>

            </Setter>

        </Style>




    </Window.Resources>
    
    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Loaded">
            <i:InvokeCommandAction Command="{Binding Path=cmdWindowLoadEvent}" CommandParameter="{Binding Text, ElementName=textBox}" />
        </i:EventTrigger>
    </i:Interaction.Triggers>
    <Window.TaskbarItemInfo>
        <TaskbarItemInfo Description="Chronos">
        </TaskbarItemInfo>
    </Window.TaskbarItemInfo>
    <Grid x:Name="LayoutRoot" Background="#E1EAF6"> <!-- Background="#E1EAF6" {res:ReflectionResourcesRef ResourceKey=DesktopFooter}-->
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid x:Name="ShellHeader" Background="{res:ReflectionResourcesRef ResourceKey=DesktopHeader}">
            <Grid.Style>
                <Style>
                    <Style.Triggers>
                        <Trigger Property="UIElement.IsMouseOver" Value="True">
                            <Setter Property="Panel.ZIndex" Value="1" />
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </Grid.Style>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Grid x:Name="WindowHeader" Height="Auto" Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="50*" />
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0"
                            Margin="5,0,0,0"
                            Orientation="Horizontal"
                            HorizontalAlignment="Left"
                            VerticalAlignment="Top">
                    <StackPanel Orientation="Vertical" HorizontalAlignment="Left" VerticalAlignment="Center">
                        <StackPanel>
                            <TextBlock x:Name="textBox" Text="REFLECTION" Foreground="LightGray" FontFamily="Agency FB" FontSize="16" FontWeight = "Bold" Background="Transparent"/>
                            <Rectangle Height="{Binding ElementName=textBox, Path=ActualHeight}" Width="{Binding ElementName=textBox, Path=ActualWidth}">
                                <Rectangle.Fill>
                                    <VisualBrush Visual="{Binding ElementName=textBox}"/>
                                </Rectangle.Fill>
                                <Rectangle.LayoutTransform>
                                    <ScaleTransform ScaleY="-1"/>
                                </Rectangle.LayoutTransform>
                                <Rectangle.OpacityMask>
                                    <LinearGradientBrush EndPoint="0,1">
                                        <GradientStop Offset="0" Color="Transparent"/>
                                        <GradientStop Offset="1" Color="#77000000"/>
                                    </LinearGradientBrush>
                                </Rectangle.OpacityMask>
                            </Rectangle>
                        </StackPanel>
                        <!--<TextBlock x:Name="WindowTitle"
                               FontFamily="{res:ReflectionResourcesRef ResourceKey=ApplicationNameFontFamily}" FontSize="{res:ReflectionResourcesRef ResourceKey=ApplicationNameFontSize}" Foreground="{res:ReflectionResourcesRef ResourceKey=ApplicationNameBrush}" FontWeight="Bold"
                               IsHitTestVisible="False"
                               HorizontalAlignment="Left" VerticalAlignment="Center" Margin="0,2,0,0"
                               Opacity="0.5"
                               Text="REFLECTION 1.0 BETA">
                        </TextBlock>-->
                    </StackPanel>


                    <TextBlock Margin="5,0,5,0"
                               FontFamily="{res:ReflectionResourcesRef ResourceKey=ApplicationNameFontFamily}" FontSize="{res:ReflectionResourcesRef ResourceKey=ApplicationNameFontSize}" Foreground="{res:ReflectionResourcesRef ResourceKey=ApplicationNameBrush}" FontWeight="Bold"
                               IsHitTestVisible="False"
                               HorizontalAlignment="Left" VerticalAlignment="Top" 
                               Opacity="0.5"
                               Text="|">
                    </TextBlock>

                    <TextBlock x:Name="UserName"
                               FontSize="12" FontWeight="Bold" Foreground="Aqua"
                               Text="{Binding UserName}"
                               HorizontalAlignment="Left" VerticalAlignment="Top" Margin="0,2,0,0"
                               ToolTip="Active user"/>
                    
                    


                </StackPanel>

                <TextBlock x:Name="Company" Grid.Column="1" FontSize="12" FontWeight="Bold" Foreground="Aqua" 
                               Text="{Binding CurrentCompany,UpdateSourceTrigger=PropertyChanged}"
                               HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,2,0,0"
                               ToolTip="Current Company" Width="450" TextWrapping="NoWrap" />

                <StackPanel Grid.Column="2"
                            Orientation="Horizontal"
                            HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,0,5,0">
                    
                    <Button x:Name="ShowPOS" Style="{StaticResource GlassButton}" Height="30" Width="30"
                            Command="{Binding SwitchDesktopCommand}"
                            VerticalAlignment="Center"
                            Margin="10,3.5,5.5,3.5"
                            ToolTip="Open POS System">
                        <Image Source="{StaticResource imgPOS}" Height="23" Width="23"/>
                    </Button>
                    
                    <Button x:Name="ShowDesktopButton" Style="{StaticResource GlassButton}" Height="30" Width="30"
                            Command="{Binding ShowDesktopCommand}"
                            VerticalAlignment="Center"
                            Margin="3.5,3.5,5.5,3.5"
                            ToolTip="Settings and Configuration">
                        <Image Source="{StaticResource imgSetttings}" Height="20" Width="20"/>
                    </Button>
                    <Button x:Name="ShowWidgetLibraryMenuItem" Style="{StaticResource GlassButton}" Height="30" Width="30"
                            Command="{Binding ShowWidgetLibraryCommand}"
                            VerticalAlignment="Center"
                            Margin="10,3.5,5.5,3.5"
                            ToolTip="Open Widget">
                        <Image Source="{StaticResource imgNavigation}" Height="23" Width="23"/>
                    </Button>
                    <Button x:Name="SaveDesktop" Style="{StaticResource GlassButton}" Height="30" Width="30"
                            Command="{Binding SaveCurrentDesktopCommand}"
                            VerticalAlignment="Center"
                            Margin="10,3.5,5.5,3.5"
                            ToolTip="Save desktop">
                        <Image Source="{StaticResource imgSaveDesktop}" Height="23" Width="23"/>
                    </Button>
                    <Button x:Name="btnHelp" Style="{StaticResource GlassButton}" Height="30" Width="30"
                            VerticalAlignment="Center"
                            ToolTip="Help"
                            Margin="10,3.5,5.5,3.5"
                            >
                        <Image Source="{StaticResource imgHelp}" Height="23" Width="23"/>
                    </Button>
                    <Button x:Name="MinimizeButton"
                            Command="{Binding MinimizeCommand}"
                            VerticalAlignment="Center"
                            Margin="30,1.5,1.5,1.5"
                            Style="{res:ReflectionResourcesRef ResourceKey=MetroButtonStyle}"
                            ToolTip="Minimize">
                        <TextBlock TextWrapping="Wrap" Text="0" FontFamily="Webdings" FontSize="13.333" Foreground="{res:ReflectionResourcesRef ResourceKey=WhiteColorBrush}" />
                    </Button>
                    <Button x:Name="CloseButton"
                            Command="{Binding ShutdownCommand}"
                            VerticalAlignment="Center"
                            Margin="1.5"
                            Style="{res:ReflectionResourcesRef ResourceKey=MetroButtonStyle}"
                            ToolTip="Close">
                        <TextBlock TextWrapping="Wrap" Text="r" FontFamily="Webdings" FontSize="13.333" Foreground="{res:ReflectionResourcesRef ResourceKey=WhiteColorBrush}" />
                    </Button>
                </StackPanel>
            </Grid>

            <Grid x:Name="TaskBar" Grid.Row="2" Height="50" HorizontalAlignment="Left">
                <Grid.RowDefinitions>
                    <RowDefinition/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <ScrollViewer Grid.Row="0" Grid.Column="1" Style="{res:ReflectionResourcesRef ResourceKey=TaskBarScrollViewerStyle}"
                              HorizontalScrollBarVisibility="Auto"
                              VerticalScrollBarVisibility="Disabled">
                    <ItemsControl x:Name="TaskBarItems"
                              FocusVisualStyle="{x:Null}"
                              AllowDrop="True"
                              BorderThickness="0"
                              Background="Transparent"
                              ItemsSource="{Binding ActiveWindows}"
                              ItemsPanel="{res:ReflectionResourcesRef ResourceKey=TaskBarPanelTemplate}"
                              ItemTemplate="{res:ReflectionResourcesRef ResourceKey=TaskBarItemTemplate}"
                              VerticalContentAlignment="Center">
                    </ItemsControl>
                </ScrollViewer>
                <StackPanel Grid.Row="0" Grid.Column="0" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center" MinWidth="500">
                    <Button Height="35" Width="35" Margin="10,0,20,0" x:Name="btnMessage" Style="{StaticResource NotificationButton}">
                        <StackPanel Orientation="Horizontal">
                            <Image Source="{StaticResource imgMessenger}" Margin="5" />
                        </StackPanel>
                    </Button>
                    <Button Height="35" Width="35" Margin="10,0,20,0" Command="{Binding ShowMassengerCommand}" x:Name="btnMails" Style="{StaticResource NotificationButton}" ToolTip="Open Massenger">
                        <StackPanel Orientation="Horizontal">
                            <Image Source="{StaticResource imgMail}" Margin="5" />
                        </StackPanel>
                    </Button>

                    <Button Height="35" Width="35" Margin="10,0,10,0" Command="{Binding ShowTaskManagerCommand}" x:Name="btnTaskManager" Style="{StaticResource NotificationButton}" ToolTip="Open Task Manager">
                        <Image  Source="{StaticResource imgClock}"/>
                    </Button>
                </StackPanel>

            </Grid>
        </Grid>

        <chronos:Desktop x:Name="VirtualDesktop01"
                         Grid.Row="3"
                         FocusVisualStyle="{x:Null}"
                         Visibility="Collapsed"
                         Background="Transparent"
                         AllowDrop="True"
                         Id="94fa801f-bef7-437a-ba75-352f1ede6fb9"
                         chronos:VirtualDesktopManager.IsDesktop="True"
                         chronos:DragDropManager.IsDropTarget="True">
            
        </chronos:Desktop>

        <chronos:Desktop x:Name="VirtualDesktop02"
                         Grid.Row="3"
                         FocusVisualStyle="{x:Null}"
                         Visibility="Collapsed"
                         Background="Transparent"
                         AllowDrop="True"
                         Id="2afc760e-9450-448d-9f28-5311c49e4773"
                         chronos:VirtualDesktopManager.IsDesktop="True"
                         chronos:DragDropManager.IsDropTarget="True">
            
            <Grid x:Name="MainGridPOS"  
                  Height="{Binding ActualHeight,ElementName=VirtualDesktop02}"
                  Width="{Binding ActualWidth,ElementName=VirtualDesktop02}"
                  >

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <!--<ColumnDefinition Width="*" />-->
                </Grid.ColumnDefinitions>

                <!--// Navigation Panel Background="#2E333A" //-->

                <Grid x:Name="nav_pnl" Grid.Column="0" HorizontalAlignment="Left" Width="50" Background="#1F497D" > <!--Background="#FF6DB4EF"   Background="#1F497D" >-->
                    <!--Background="#2E333A"-->

                    <StackPanel x:Name="st_pnl">

                        <!--// Grid for the Menu Toggle Button //-->

                        <Grid Height="100">

                            <!--// Menu title text //-->

                            <TextBlock  Grid.Row="1"
                                    Grid.Column="0"
                                    Margin="73,-20,0,0"
                                    Text="M E N U"
                                    Foreground="White"
                                    FontSize="22"
                                    Background="Transparent"
                                    HorizontalAlignment="Left"
                                    VerticalAlignment="Center"
                                    Style="{StaticResource font_style}">
                            
                            <!--// Shadow Effect //-->

                                <TextBlock.Effect>

                                    <DropShadowEffect  BlurRadius="10"
                                                   ShadowDepth="1"
                                                   Direction="-90"
                                                   Color="White"/>

                                </TextBlock.Effect>

                            </TextBlock>

                            <!--// Toggle Button //-->

                            <ToggleButton x:Name="Tg_Btn"
                                      Grid.Column="0"
                                      VerticalAlignment="Center"
                                      HorizontalAlignment="Left"
                                      Margin="10,-20,0,0"
                                      Height="30"
                                      Width="30"
                                      BorderThickness="0"
                                      Style="{StaticResource tb_style}" Unchecked="Tg_Btn_Unchecked" Checked="Tg_Btn_Checked" >

                                <!--// Toggle button default background image //-->

                                <ToggleButton.Background>

                                    <ImageBrush ImageSource="{StaticResource imgMenu}"
                                            Stretch="None"/>

                                </ToggleButton.Background>

                                <!--// Toggle Button - Checked, Unchecked events //-->

                                <ToggleButton.Triggers>

                                    <EventTrigger RoutedEvent="ToggleButton.Unchecked">

                                        <BeginStoryboard>

                                            <Storyboard x:Name="HideStackPanel">

                                                <DoubleAnimation    
                                                
                                                Storyboard.TargetName="nav_pnl"
                                                Storyboard.TargetProperty="Width"
                                                BeginTime="0:0:0"
                                                From="230" To="50"
                                                Duration="0:0:0.2">

                                                </DoubleAnimation>

                                            </Storyboard>

                                        </BeginStoryboard>

                                    </EventTrigger>

                                    <EventTrigger RoutedEvent="ToggleButton.Checked">

                                        <BeginStoryboard>

                                            <Storyboard x:Name="ShowStackPanel">

                                                <DoubleAnimation
                                                
                                                Storyboard.TargetName="nav_pnl"
                                                Storyboard.TargetProperty="Width"
                                                BeginTime="0:0:0"
                                                From="50" To="230"
                                                Duration="0:0:0.3">

                                                </DoubleAnimation>

                                            </Storyboard>

                                        </BeginStoryboard>

                                    </EventTrigger>

                                </ToggleButton.Triggers>


                            </ToggleButton>

                        </Grid>

                        <!--// ListView with menu list items //-->

                        <ListView  x:Name="LV" Background="Transparent" BorderBrush="Transparent" ScrollViewer.HorizontalScrollBarVisibility="Disabled">

                            <!--// Home //-->

                            <ListViewItem HorizontalAlignment="Left"
                                      Margin="0,0,0,15" MouseEnter="ListViewItem_MouseEnter"
                                      >

                                <StackPanel Orientation="Horizontal"
                                        Width="230">

                                    <Image Source="{StaticResource imgHome}" Width="15" Height="15"
                                       Stretch="None"
                                       Margin="0,0,0,0"/>

                                    <TextBlock Text="Home"
                                           Margin="25,0,0,0"
                                           Style="{StaticResource font_style}"/>

                                </StackPanel>

                                <!--// Tooltip //-->

                                <ListViewItem.ToolTip>

                                    <ToolTip x:Name="tt_home"
                                         Content="Home"
                                         Style="{StaticResource tt_style}"/>

                                </ListViewItem.ToolTip>

                            </ListViewItem>

                            <!--// Contacts //-->

                            <ListViewItem HorizontalAlignment="Left"
                                      Margin="0,0,0,15" MouseEnter="ListViewItem_MouseEnter"
                                      >

                                <StackPanel Orientation="Horizontal"
                                        Width="230">

                                    <Image Source="{StaticResource imgAdd2}" 
                                       Stretch="None"
                                       Margin="0,0,0,0"/>

                                    <TextBlock Text="Contacts"
                                           Margin="25,0,0,0"
                                           Style="{StaticResource font_style}"/>

                                </StackPanel>

                                <!--// Tooltip //-->

                                <ListViewItem.ToolTip>

                                    <ToolTip x:Name="tt_contacts"
                                         Content="Contacts"
                                         Style="{StaticResource tt_style}"/>

                                </ListViewItem.ToolTip>

                            </ListViewItem>

                            <!--// Messages //-->

                            <ListViewItem HorizontalAlignment="Left"
                                      Margin="0,0,0,15" MouseEnter="ListViewItem_MouseEnter"
                                      >

                                <StackPanel Orientation="Horizontal"
                                        Width="230">

                                    <Image Source="{StaticResource imgWatch}" 
                                       Stretch="None"
                                       Margin="0,0,0,0"/>

                                    <TextBlock Text="Messages"
                                           Margin="25,0,0,0"
                                           Style="{StaticResource font_style}"/>

                                </StackPanel>

                                <!--// Tooltip //-->

                                <ListViewItem.ToolTip>

                                    <ToolTip x:Name="tt_messages"
                                         Content="Messages"
                                         Style="{StaticResource tt_style}"/>

                                </ListViewItem.ToolTip>

                            </ListViewItem>

                            <!--// Maps //-->

                            <ListViewItem HorizontalAlignment="Left"
                                      Margin="0,0,0,15" MouseEnter="ListViewItem_MouseEnter"
                                      >

                                <StackPanel Orientation="Horizontal"
                                        Width="230">

                                    <Image Source="{StaticResource imgMail}"  Width="25" Height="25"
                                       Stretch="None"
                                       Margin="0,0,0,0"/>

                                    <TextBlock Text="Maps"
                                           Margin="25,0,0,0"
                                           Style="{StaticResource font_style}"/>

                                </StackPanel>

                                <!--// Tooltip //-->

                                <ListViewItem.ToolTip>

                                    <ToolTip x:Name="tt_maps"
                                         Content="Maps"
                                         Style="{StaticResource tt_style}"/>

                                </ListViewItem.ToolTip>

                            </ListViewItem>

                            <!--// Settings //-->

                            <ListViewItem HorizontalAlignment="Left"
                                      Margin="0,0,0,15" MouseEnter="ListViewItem_MouseEnter"
                                      >

                                <StackPanel Orientation="Horizontal"
                                        Width="230">

                                    <Image Source="{StaticResource imgSetting}" 
                                       Stretch="None"
                                       Margin="0,0,0,0"/>

                                    <TextBlock Text="Settings"
                                           Margin="25,0,0,0"
                                           Style="{StaticResource font_style}"/>

                                </StackPanel>

                                <!--// Tooltip //-->

                                <ListViewItem.ToolTip>

                                    <ToolTip x:Name="tt_settings"
                                         Content="Settings"
                                         Style="{StaticResource tt_style}"/>

                                </ListViewItem.ToolTip>

                            </ListViewItem>

                            <!--// Sign Out //-->

                            <ListViewItem HorizontalAlignment="Left"
                                      Margin="0,0,0,15" MouseEnter="ListViewItem_MouseEnter"
                                      >

                                <StackPanel Orientation="Horizontal" Width="230">

                                    <Image Source="{StaticResource imgStartBullet}"
                                       Stretch="None" Margin="0,0,0,0"/>

                                    <TextBlock Text="Sign Out"  Margin="25,0,0,0" Style="{StaticResource font_style}"/>

                                </StackPanel>

                                <!--// Tooltip //-->

                                <ListViewItem.ToolTip>

                                    <ToolTip x:Name="tt_signout"
                                         Content="Sign Out"
                                         Style="{StaticResource tt_style}"/>

                                </ListViewItem.ToolTip>

                            </ListViewItem>

                            <!--// Sign Out //-->

                            <ListViewItem HorizontalAlignment="Left" Margin="0,0,0,15" MouseEnter="ListViewItem_MouseEnter" >
                                <StackPanel Orientation="Horizontal" Width="230">
                                    <Image Source="{StaticResource imgWatch}" Stretch="None" Margin="0,0,0,0"  />
                                    <TextBlock Text="Message" Margin="25,0,0,0" Style="{StaticResource font_style}"/>
                                </StackPanel>
                                <!--// Tooltip //-->
                                <ListViewItem.ToolTip>
                                    <ToolTip x:Name="tt_messages2" Content="Sign Out" Style="{StaticResource tt_style}"/>
                                </ListViewItem.ToolTip>
                            </ListViewItem>

                            <!--// Maps //-->

                            <ListViewItem HorizontalAlignment="Left"
                                      Margin="0,0,0,15" MouseEnter="ListViewItem_MouseEnter"
                                      >

                                <StackPanel Orientation="Horizontal"
                                        Width="230">

                                    <Image Source="{StaticResource imgWorkflow}" Width="20" Height="20"
                                       Stretch="None"
                                       Margin="0,0,0,0"/>

                                    <TextBlock Text="Maps"
                                           Margin="25,0,0,0"
                                           Style="{StaticResource font_style}"/>

                                </StackPanel>

                                <!--// Tooltip //-->

                                <ListViewItem.ToolTip>

                                    <ToolTip x:Name="tt_maps2"
                                         Content="Maps"
                                         Style="{StaticResource tt_style}"/>

                                </ListViewItem.ToolTip>

                            </ListViewItem>

                            <!--// Settings //-->

                            <ListViewItem HorizontalAlignment="Left"
                                      Margin="0,0,0,15" MouseEnter="ListViewItem_MouseEnter"
                                      >

                                <StackPanel Orientation="Horizontal"
                                        Width="230">

                                    <Image Source="{StaticResource imgSetting2}" 
                                       Stretch="None"
                                       Margin="0,0,0,0"/>

                                    <TextBlock Text="Settings"
                                           Margin="25,0,0,0"
                                           Style="{StaticResource font_style}"/>

                                </StackPanel>

                                <!--// Tooltip //-->

                                <ListViewItem.ToolTip>

                                    <ToolTip x:Name="tt_settings2"
                                         Content="Settings"
                                         Style="{StaticResource tt_style}"/>

                                </ListViewItem.ToolTip>

                            </ListViewItem>


                            <!--// Sign Out //-->

                            <ListViewItem HorizontalAlignment="Left" Margin="0,0,0,15" MouseEnter="ListViewItem_MouseEnter" >
                                <StackPanel Orientation="Horizontal" Width="230">
                                    <Image Source="{StaticResource imgSignout}" Stretch="None" Margin="0,0,0,0" />
                                    <TextBlock Text="Sign Out" Margin="25,0,0,0" Style="{StaticResource font_style}"/>
                                </StackPanel>
                                <!--// Tooltip //-->
                                <ListViewItem.ToolTip>
                                    <ToolTip x:Name="tt_signout2" Content="Sign Out" Style="{StaticResource tt_style}"/>
                                </ListViewItem.ToolTip>
                            </ListViewItem>



                            <!--// Listview items style template //-->

                            <ListView.ItemContainerStyle>

                                <Style TargetType="ListViewItem">

                                    <Setter Property="Template">

                                        <Setter.Value>

                                            <ControlTemplate TargetType="{x:Type ListViewItem}">

                                                <Border x:Name="Bd"
                                                    Height="50"
                                                    BorderBrush="{TemplateBinding BorderBrush}"
                                                    BorderThickness="{TemplateBinding BorderThickness}"
                                                    Background="{TemplateBinding Background}"
                                                    Padding="{TemplateBinding Padding}"
                                                    SnapsToDevicePixels="True">

                                                    <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                                  SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"
                                                                  VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>

                                                </Border>

                                                <ControlTemplate.Triggers>

                                                    <MultiTrigger>

                                                        <MultiTrigger.Conditions>

                                                            <Condition Property="IsMouseOver" Value="True"/>

                                                        </MultiTrigger.Conditions>

                                                        <Setter Property="Background"
                                                            TargetName="Bd"
                                                            Value="#404D5B"/>

                                                        <Setter Property="BorderBrush"
                                                            TargetName="Bd"
                                                            Value="#404D5B"/>

                                                    </MultiTrigger>

                                                </ControlTemplate.Triggers>

                                            </ControlTemplate>

                                        </Setter.Value>

                                    </Setter>

                                </Style>

                            </ListView.ItemContainerStyle>

                        </ListView>

                    </StackPanel>

                </Grid>


                <!--// Workspace //-->

                <Grid x:Name="WorkSpace" Grid.Column="1" 
                      Height="{Binding ActualHeight,ElementName=MainGridPOS}" 
                      Width="{Binding ActualWidth,ElementName=MainGridPOS}"
                      > <!--Background="#E1EAF6"-->
                    <ContentControl x:Name="ccWorkspace"  />
                    <!--<ContentControl x:Name="ccWorkspace" Content="{Binding UC_OBJ}" Grid.Column="0" Grid.Row="1" />-->
                </Grid>


            </Grid>
            
        </chronos:Desktop>

        <chronos:Desktop x:Name="ModalContainer" 
                         Grid.RowSpan="4" 
                         FocusVisualStyle="{x:Null}"
                         AllowDrop="False" 
                         Visibility="Collapsed"
                         chronos:VirtualDesktopManager.IsModalContainer="True">
        </chronos:Desktop>

        <StackPanel Grid.Row="2"  Orientation="Vertical" HorizontalAlignment="Right" VerticalAlignment="Bottom">
            <Image Source="{Binding CompanyLogo}" MaxWidth="200" MaxHeight="100" Stretch="Fill" Opacity="0.5" Margin="0,0,20,0"/>
            <!--<TextBlock Text="{Binding CurrentCompany}"/>-->
        </StackPanel>
        
        <!--<Grid.Background>
            <ImageBrush ImageSource="{Binding CompanyLogo}" MaxWidth="200" MaxHeight="100"
                    Opacity="0.5"
                    Stretch="UniformToFill"  />
        </Grid.Background>-->

    </Grid>
</Window>
