<Window x:Class="Reflection.Presentation.Controls.UserControls.AddressControl"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Reflection.Presentation.Controls.UserControls"
        mc:Ignorable="d"
        Title="AddressControl" Height="450" Width="800">
    <Grid HorizontalAlignment="Left" MinWidth="300">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="Bill Amt" VerticalAlignment="Center" Foreground="SlateGray" FontSize="18" FontWeight="Bold" Margin="0,0,0,0" />
        <TextBox x:Name="txtAmount" Grid.Row="0" Grid.Column="1" MaxWidth="200" MinWidth="130" MinHeight="40" Text="{Binding MasterEntity.add_code,Mode=TwoWay ,UpdateSourceTrigger=PropertyChanged }"  HorizontalAlignment="Left" VerticalAlignment="Center" HorizontalContentAlignment="Right" VerticalContentAlignment="Center" FontWeight="Bold" FontSize="18" Margin="5,0,20,0" TabIndex="3" />

        <TextBlock Grid.Row="1" Grid.Column="0" Text="Recd. Amt"  VerticalAlignment="Center" Foreground="SlateGray" FontSize="18" FontWeight="Bold" Margin="0,0,0,0"/>
        <TextBox x:Name="txtPaid" Grid.Row="1" Grid.Column="1" MaxWidth="200" MinWidth="130" MinHeight="40" Text="{Binding MasterEntity.obj_code,Mode=TwoWay ,UpdateSourceTrigger=PropertyChanged }" HorizontalAlignment="Left" VerticalAlignment="Center" HorizontalContentAlignment="Right" VerticalContentAlignment="Center" FontWeight="Bold" FontSize="18" Margin="5,0,20,0"  TabIndex="3" />

        <TextBlock Grid.Row="2" Grid.Column="0" Text="Chenge"  VerticalAlignment="Center" Foreground="SlateGray"  FontSize="18" FontWeight="Bold" Margin="0,0,0,0"/>
        <TextBox x:Name="txtChange" Grid.Row="2" Grid.Column="1" MaxWidth="200" MinWidth="130" MinHeight="40" Text="{Binding MasterEntity.obj_type,Mode=TwoWay ,UpdateSourceTrigger=PropertyChanged }"  HorizontalAlignment="Left" VerticalAlignment="Center" HorizontalContentAlignment="Right" VerticalContentAlignment="Center" FontWeight="Bold" FontSize="18" Margin="5,0,20,0" TabIndex="3" />

        <Button Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="4" Content="Process Payment" Command="{Binding Path=cmdSavePayment}" CommandParameter="POS_SAVE" Style="{StaticResource FlatButton}" Background="SlateGray" Width="200" Height="40" FontWeight="Bold" FontSize="20" Margin="0,0,30,0" VerticalAlignment="Bottom" HorizontalAlignment="Center" />

    </Grid>
</Window>
