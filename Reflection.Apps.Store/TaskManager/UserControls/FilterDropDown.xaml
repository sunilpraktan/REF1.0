<UserControl x:Class="Reflection.Apps.Store.TaskManager.FilterDropDown"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:tm="clr-namespace:Reflection.Apps.Store.TaskManager"
             >
    <UserControl.Resources>
        <Style x:Key="DropDownSelectorButton" TargetType="{x:Type Button}">
            <Setter Property="SnapsToDevicePixels" Value="true" />
            <Setter Property="OverridesDefaultStyle" Value="true" />
            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="FontSize" Value="16" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="FontWeight" Value="Light" />
            <Setter Property="Padding" Value="18,10" />
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Grid Background="Transparent">
                            <Border x:Name="Border" BorderThickness="0,1" SnapsToDevicePixels="true">
                                <Border.BorderBrush>
                                    <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                        <GradientStop Offset="0" Color="#18CCCCCC" />
                                        <GradientStop Offset="1" Color="#60000000" />
                                    </LinearGradientBrush>
                                </Border.BorderBrush>
                                <ContentPresenter Margin="{TemplateBinding Padding}" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" />
                            </Border>
                            <Border x:Name="BottomShadow" Height="1" Margin="0,0,0,-1" VerticalAlignment="Bottom" Background="#4000" Visibility="Hidden" />
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="tm:IndexingStackPanel.StackLocation" Value="First">
                                <Setter TargetName="Border" Property="CornerRadius" Value="0,4,0,0" />
                                <Setter TargetName="Border" Property="BorderThickness" Value="0,0,0,1" />
                            </Trigger>
                            <Trigger Property="tm:IndexingStackPanel.StackLocation" Value="Last">
                                <Setter TargetName="Border" Property="CornerRadius" Value="0,0,4,4" />
                                <Setter TargetName="Border" Property="BorderThickness" Value="0,1,0,0" />
                            </Trigger>
                            <Trigger Property="Tag" Value="First">
                                <Setter TargetName="Border" Property="CornerRadius" Value="0,4,0,0" />
                                <Setter TargetName="Border" Property="BorderThickness" Value="0,0,0,1" />
                            </Trigger>
                            <Trigger Property="Tag" Value="Last">
                                <Setter TargetName="Border" Property="CornerRadius" Value="0,0,4,4" />
                                <Setter TargetName="Border" Property="BorderThickness" Value="0,1,0,0" />
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="true">
                                <Setter TargetName="Border" Property="Background">
                                    <Setter.Value>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                            <GradientStop Offset="0" Color="#131516" />
                                            <GradientStop Offset="1" Color="#131516" />
                                        </LinearGradientBrush>
                                    </Setter.Value>
                                </Setter>
                                <Setter TargetName="Border" Property="BorderBrush">
                                    <Setter.Value>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                            <GradientStop Offset="0" Color="#383b3c" />
                                            <GradientStop Offset="1" Color="#FF000000" />
                                        </LinearGradientBrush>
                                    </Setter.Value>
                                </Setter>
                                <Setter TargetName="BottomShadow" Property="Visibility" Value="Visible" />

                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    <Grid x:Name="LayoutRoot" >

        <StackPanel Orientation="Horizontal">
            <tm:PopupToggle Content="{Binding FilterDescription}" Popup="{Binding ElementName=Popup}" Foreground="White" FontWeight="Light" FontSize="18" >
                <tm:PopupToggle.Style>
                    <Style TargetType="tm:PopupToggle">
                        <Setter Property="OverridesDefaultStyle" Value="true" />
                        <Setter Property="Cursor" Value="Hand" />
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="tm:PopupToggle">
                                    <Border x:Name="Border" Background="#00FFC410" CornerRadius="2" >
                                        <Grid Margin="8,3" >
                                            <TextBlock Margin="12,8,16,8">
                                                <TextBlock Text="Show" Foreground="#8e9396" />
                                                <TextBlock Margin="2,0,0,0"  Text="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=Content}" />
                                            </TextBlock>
                                            <Path Width="8" Height="4" Data="{StaticResource DownArrow}" Stretch="Fill" Fill="White" VerticalAlignment="Center" HorizontalAlignment="Right" />

                                        </Grid>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </tm:PopupToggle.Style>

            </tm:PopupToggle>
        </StackPanel>

        <Popup x:Name="Popup" AllowsTransparency="True" Focusable="False" Placement="Custom" CustomPopupPlacementCallback="{x:Static tm:PopupHelper.SimplePlacementCallback}" PopupAnimation="Fade" HorizontalOffset="44" VerticalOffset="2" >
            <Grid Name="DropDown" MinWidth="225" SnapsToDevicePixels="True" Margin="20" >

                <tm:CalloutShape ArrowPlacement="Top" ArrowAlignment="Left" ArrowWidth="25" ArrowHeight="25" Stroke="Black" CornerRadius="4" >
                    <tm:CalloutShape.Fill>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                            <GradientStop Offset="0" Color="#E8383C3E"/>
                            <GradientStop Offset="1" Color="#E8222627"/>
                        </LinearGradientBrush>
                    </tm:CalloutShape.Fill>
                    <tm:CalloutShape.Effect>
                        <DropShadowEffect BlurRadius="15" Direction="-90" Opacity="0.5" ShadowDepth="5"/>
                    </tm:CalloutShape.Effect>
                </tm:CalloutShape>

                <StackPanel Margin="1,26,1,1">

                    <ItemsControl DataContext="{Binding Source={x:Static tm:TaskManagerView.Root}, Path=Filters}" ItemsSource="{Binding Items}" >
                        <ItemsControl.Template>
                            <ControlTemplate>
                                <tm:IndexingStackPanel IsItemsHost="True" />
                            </ControlTemplate>
                        </ItemsControl.Template>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Grid>
                                    <Button 
                                    x:Name="Button"
                                    Content="{Binding}"
                                    Style="{StaticResource DropDownSelectorButton}" 
                                    Click="Button_Click" 
                                    Command="{Binding Path=DataContext.SetCurrentCommand, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type ItemsControl}}}"
                                    CommandParameter="{Binding}" />
                                    <Path x:Name="Checkmark" Width="15" Height="18" Margin="0,0,10,0" Data="M0,0.6 L0.4,1 L1,0" Opacity="0" Stretch="Fill" Stroke="White" StrokeThickness="2.5" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                                </Grid>
                                <DataTemplate.Triggers>
                                    <Trigger SourceName="Button" Property="IsEnabled" Value="False">
                                        <Setter TargetName="Checkmark" Property="Opacity" Value="1" />
                                    </Trigger>
                                </DataTemplate.Triggers>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>

                </StackPanel>
                <tm:CalloutShape ArrowPlacement="Top" ArrowAlignment="Left" ArrowWidth="23" ArrowHeight="24" Stroke="#20FFFFFF" CornerRadius="4" Margin="1,2,1,1" IsHitTestVisible="False" />

            </Grid>
        </Popup>
    </Grid>
</UserControl>
