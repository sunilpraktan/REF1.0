<UserControl x:Class="Reflection.Apps.Store.TaskManager.DateDropDown"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:tm="clr-namespace:Reflection.Apps.Store.TaskManager" >
    <UserControl.Resources>

        <!-- Note: the value of ShowOverDue on this converter gets changed in code -->
        <tm:PrettyDateConverter x:Key="PrettyDateConverter" ShowOverdue="False"/>
        <!--<Style x:Key="MissingValueTextBlock" TargetType="{x:Type TextBlock}">
            --><!--<Setter Property="FontStyle" Value="Italic" />--><!--
            <Setter Property="FontWeight" Value="Bold" />
            <Setter Property="Opacity" Value="0.5" />
        </Style>-->
        <Style x:Key="DefaultPopupToggle" TargetType="{x:Type tm:PopupToggle}">
            <Setter Property="OverridesDefaultStyle" Value="true" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type tm:PopupToggle}">
                        <Border x:Name="Border" Background="#00FFC410" CornerRadius="2">
                            <Grid Margin="8,3">
                                <ContentPresenter />
                                <TextBlock x:Name="InformationTextBlock" Style="{StaticResource MissingValueTextBlock}" Text="{TemplateBinding tm:Attached.InformationText}" Visibility="Collapsed" />
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <!--<Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="#20FFC410"/>
                        </Trigger>-->
                            <!--<Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="#60FFC410"/>
                        </Trigger>-->
                            <Trigger Property="Content" Value="{x:Null}">
                                <Setter TargetName="InformationTextBlock" Property="Visibility" Value="Visible" />
                            </Trigger>
                            <DataTrigger Binding="{Binding Content.Length, RelativeSource={RelativeSource Self}}" Value="0">
                                <Setter TargetName="InformationTextBlock" Property="Visibility" Value="Visible" />
                            </DataTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="DropDownSelectorButton" TargetType="{x:Type Button}">
            <Setter Property="SnapsToDevicePixels" Value="true" />
            <Setter Property="OverridesDefaultStyle" Value="true" />
            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="FontSize" Value="16" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="FontWeight" Value="Light" />
            <Setter Property="Padding" Value="18,10" />
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Grid Background="Transparent">
                            <Border x:Name="Border" BorderThickness="0,1" SnapsToDevicePixels="true">
                                <Border.BorderBrush>
                                    <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                        <GradientStop Offset="0" Color="#18CCCCCC" />
                                        <GradientStop Offset="1" Color="#60000000" />
                                    </LinearGradientBrush>
                                </Border.BorderBrush>
                                <ContentPresenter Margin="{TemplateBinding Padding}" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" />
                            </Border>
                            <Border x:Name="BottomShadow" Height="1" Margin="0,0,0,-1" VerticalAlignment="Bottom" Background="#4000" Visibility="Hidden" />
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="tm:IndexingStackPanel.StackLocation" Value="First">
                                <Setter TargetName="Border" Property="CornerRadius" Value="0,4,0,0" />
                                <Setter TargetName="Border" Property="BorderThickness" Value="0,0,0,1" />
                            </Trigger>
                            <Trigger Property="tm:IndexingStackPanel.StackLocation" Value="Last">
                                <Setter TargetName="Border" Property="CornerRadius" Value="0,0,4,4" />
                                <Setter TargetName="Border" Property="BorderThickness" Value="0,1,0,0" />
                            </Trigger>
                            <Trigger Property="Tag" Value="First">
                                <Setter TargetName="Border" Property="CornerRadius" Value="0,4,0,0" />
                                <Setter TargetName="Border" Property="BorderThickness" Value="0,0,0,1" />
                            </Trigger>
                            <Trigger Property="Tag" Value="Last">
                                <Setter TargetName="Border" Property="CornerRadius" Value="0,0,4,4" />
                                <Setter TargetName="Border" Property="BorderThickness" Value="0,1,0,0" />
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="true">
                                <Setter TargetName="Border" Property="Background">
                                    <Setter.Value>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                            <GradientStop Offset="0" Color="#131516" />
                                            <GradientStop Offset="1" Color="#131516" />
                                        </LinearGradientBrush>
                                    </Setter.Value>
                                </Setter>
                                <Setter TargetName="Border" Property="BorderBrush">
                                    <Setter.Value>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                            <GradientStop Offset="0" Color="#383b3c" />
                                            <GradientStop Offset="1" Color="#FF000000" />
                                        </LinearGradientBrush>
                                    </Setter.Value>
                                </Setter>
                                <Setter TargetName="BottomShadow" Property="Visibility" Value="Visible" />

                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    <Grid x:Name="LayoutRoot">

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Top" >
            <tm:PopupToggle Style="{StaticResource DefaultPopupToggle}" Content="{Binding Date, Converter={StaticResource PrettyDateConverter}}" Popup="{Binding ElementName=Popup}"  tm:Attached.InformationText="Due Date" Opacity="1" />
            <Border x:Name="OverdueIcon" Margin="-4,5,0,-5" Background="Transparent" ToolTip="The due date for this task is in the past." Visibility="Collapsed" Opacity="0" >
                <tm:PixelAlignedImage Source="{StaticResource WarningImageSource}" />
            </Border>
        </StackPanel>

        <Popup x:Name="Popup" AllowsTransparency="True" Focusable="False" Placement="Custom" CustomPopupPlacementCallback="{x:Static tm:PopupHelper.SimplePlacementCallback}" VerticalOffset="-10" HorizontalOffset="-15" PopupAnimation="Fade" Opened="Popup_Opened">
            <Grid Name="DropDown" MinWidth="225" Margin="20" SnapsToDevicePixels="True">

                <tm:CalloutShape ArrowPlacement="Top" ArrowAlignment="Left" ArrowWidth="25" ArrowHeight="25" Stroke="Black" CornerRadius="4" >
                    <tm:CalloutShape.Fill>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                            <GradientStop Offset="0" Color="#E8383C3E"/>
                            <GradientStop Offset="1" Color="#E8222627"/>
                        </LinearGradientBrush>
                    </tm:CalloutShape.Fill>
                    <tm:CalloutShape.Effect>
                        <DropShadowEffect BlurRadius="15" Direction="-90" Opacity="0.5" ShadowDepth="5"/>
                    </tm:CalloutShape.Effect>
                </tm:CalloutShape>

                <StackPanel Margin="1,26,1,1">

                    <Grid>
                        <TextBox x:Name="NewDateTextBox" Height="30" Margin="10" TextChanged="NewDateTextBox_TextChanged" GotFocus="NewDateTextBox_GotFocus" LostFocus="NewDateTextBox_LostFocus"  />
                        <Grid Width="20" Height="20" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,14,14,0" >
                            <tm:PixelAlignedImage x:Name="ConfirmImage" Source="{DynamicResource confirmImg}" Visibility="Collapsed" />
                            <tm:PixelAlignedImage x:Name="QuestionImage" Source="{DynamicResource questImg}" Visibility="Collapsed" />
                            <!--<c:PixelAlignedImage x:Name="ConfirmImage" Source="..\Resources\icon_confirm.png" Visibility="Collapsed" />
                            <c:PixelAlignedImage x:Name="QuestionImage" Source="..\Resources\icon_question.png" Visibility="Collapsed" />-->
                        </Grid>
                    </Grid>

                    <ItemsControl x:Name="DatesList" >
                        <ItemsControl.Template>
                            <ControlTemplate>
                                <tm:IndexingStackPanel IsItemsHost="True" />
                            </ControlTemplate>
                        </ItemsControl.Template>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Button Style="{StaticResource DropDownSelectorButton}" Click="Button_Click">
                                    <Grid Background="Transparent">
                                        <TextBlock x:Name="TextBlock" Text="{Binding Label}" Margin="0,0,36,0" HorizontalAlignment="Left" VerticalAlignment="Center" />
                                    </Grid>
                                </Button>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>

                </StackPanel>
                <tm:CalloutShape ArrowPlacement="Top" ArrowAlignment="Left" ArrowWidth="23" ArrowHeight="24" Stroke="#20FFFFFF" CornerRadius="4" Margin="1,2,1,1" IsHitTestVisible="False" />

            </Grid>
        </Popup>
    </Grid>
</UserControl>
