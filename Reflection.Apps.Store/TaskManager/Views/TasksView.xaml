<UserControl x:Class="Reflection.Apps.Store.TaskManager.TasksView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
              xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:tm="clr-namespace:Reflection.Apps.Store.TaskManager">
    <UserControl.Resources>
        <tm:NullBoolConverter x:Key="NullBoolConverter" />
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
        <Storyboard x:Key="HideScrollSlider" BeginTime="00:00:00.2">
            <DoubleAnimation Storyboard.TargetName="ScrollSlider" Storyboard.TargetProperty="Opacity" To="0" Duration="00:00:00.2" />
        </Storyboard>
        <Storyboard x:Key="ShowScrollSlider" BeginTime="00:00:01">
            <DoubleAnimation Storyboard.TargetName="ScrollSlider" Storyboard.TargetProperty="Opacity" To="1" Duration="00:00:00.2" />
        </Storyboard>

    </UserControl.Resources>

    <Grid Grid.IsSharedSizeScope="True">
        
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        
        <Border x:Name="m_revealBorder" Background="#565e63">
            <tm:EditTaskView Background="{Binding Source={x:Static tm:TaskManagerView.Root}, Path=Folders.TaskData.CurrentFolder.Color}" DataContext="{Binding NewTask}" Width="{Binding ElementName=m_revealBorder, Path=ActualWidth}" />
           
        </Border>
            
        <Border Grid.Row="1" ClipToBounds="True">
            <!--<tm:AnimatedScrollDecorator x:Name="m_scrollDecorator" IsReversed="False" Grid.Row="1" Duration="1000" DragDirection="VerticalOnly" IsDraggingEnabled="True" AnimationBehavior="OnDrag" DragScaler="0.75" RequiresScrollingChanged="m_scrollDecorator_RequiresScrollingChanged">-->
            <ScrollViewer Name="sv" VerticalScrollBarVisibility="Auto"  HorizontalScrollBarVisibility="Auto" >    
            <ListBox x:Name="m_filteredList" ItemsSource="{Binding AllTasks}" VirtualizingStackPanel.IsVirtualizing="False" >
                    <ListBox.Template>
                        <ControlTemplate TargetType="ListBox">
                            <StackPanel IsItemsHost="True" />
                        </ControlTemplate>
                    </ListBox.Template>
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="OverridesDefaultStyle" Value="True" />
                            <Setter Property="SnapsToDevicePixels" Value="True" />
                            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListBoxItem">
                                        <Grid>
                                            <ContentPresenter />
                                        </Grid>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ListBox.ItemContainerStyle>
                    <ListBox.ItemTemplate>
                        <DataTemplate DataType="{x:Type tm:TaskViewModel}">
                            <tm:TaskView Visibility="{Binding IsVisible, Converter={StaticResource BooleanToVisibilityConverter}}" />
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                    <i:Interaction.Triggers>
                        <i:EventTrigger EventName="SelectionChanged">
                            <i:InvokeCommandAction Command="{Binding Path=SelectionChangedCommand}" CommandParameter="{Binding SelectedItems, ElementName=m_filteredList}" />
                        </i:EventTrigger>
                    </i:Interaction.Triggers>
                </ListBox>
               
            </ScrollViewer>
            <!--</tm:AnimatedScrollDecorator>-->
        </Border>

        <!--<Slider x:Name="ScrollSlider" Orientation="Vertical" Style="{StaticResource ScrollSlider}" HorizontalAlignment="Right" Grid.Row="1" Margin="0,0,-22,0" Minimum="0" Maximum="1" Value="{Binding ElementName=m_scrollDecorator, Path=RelativeVerticalOffset, Mode=TwoWay}" />-->
        
    </Grid>
    
</UserControl>
