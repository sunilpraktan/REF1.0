<UserControl x:Class="Reflection.Apps.Store.TaskManager.EditTaskView" 
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:tm="clr-namespace:Reflection.Apps.Store.TaskManager" 
             xmlns:sys="clr-namespace:System;assembly=mscorlib">
    <UserControl.Resources>
        <tm:BoolToDoubleConverter x:Key="DimCompleteItemConverter" TrueValue="0.25" FalseValue="1.0" />
        <tm:BoolToDoubleConverter x:Key="HalfDimCompleteItemConverter" TrueValue="0.5" FalseValue="1.0" />
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
        <sys:String x:Key="notScheduled">Not scheduled</sys:String>
        <Style x:Key="DarkTextBox" TargetType="{x:Type TextBox}">
            <Setter Property="SnapsToDevicePixels" Value="True" />
            <Setter Property="OverridesDefaultStyle" Value="True" />
            <Setter Property="KeyboardNavigation.TabNavigation" Value="None" />
            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
            <Setter Property="FontFamily" Value="{StaticResource PrimaryFontFamily}" />
            <Setter Property="FontWeight" Value="Light" />
            <Setter Property="FontSize" Value="16" />
            <Setter Property="MinHeight" Value="30" />
            <Setter Property="Foreground" Value="#272A2B" />
            <Setter Property="MinWidth" Value="120" />
            <Setter Property="MinHeight" Value="20" />
            <Setter Property="Padding" Value="7,3,7,-3" />
            <Setter Property="AllowDrop" Value="true" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type TextBox}">
                        <Border x:Name="OuterBorder" CornerRadius="2" BorderBrush="#20FFFFFF" BorderThickness="0">
                            <Border x:Name="BackgroundBorder" BorderBrush="#80000000" BorderThickness="1" CornerRadius="1" Background="#C0FFFFFF">
                                <Grid>
                                    <Border CornerRadius="1,1,0,0" VerticalAlignment="Top" Height="4">
                                        <Border.Background>
                                            <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                                <GradientStop Color="#4000" Offset="0" />
                                                <GradientStop Color="#1000" Offset="0.5" />
                                                <GradientStop Color="#0000" Offset="1" />
                                            </LinearGradientBrush>
                                        </Border.Background>
                                    </Border>
                                    <ScrollViewer x:Name="PART_ContentHost" />
                                    <TextBlock x:Name="Message" Foreground="Black" Text="{TemplateBinding Text}" IsHitTestVisible="False" Opacity="0.35" VerticalAlignment="Center" Margin="8,0" />
                                </Grid>
                            </Border>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="tm:Attached.IsSelected" Value="True">
                                <Trigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="Message" Storyboard.TargetProperty="Opacity" To="0.1" Duration="00:00:00.2" />
                                            <ColorAnimation Storyboard.TargetName="BackgroundBorder" Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)" To="#FFFFFF" Duration="00:00:00.2" />
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.EnterActions>
                                <Trigger.ExitActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="Message" Storyboard.TargetProperty="Opacity" Duration="00:00:00.2" />
                                            <ColorAnimation Storyboard.TargetName="BackgroundBorder" Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)" Duration="00:00:00.2" />
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.ExitActions>
                            </Trigger>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <!--<Condition Property="HasText" Value="False" />-->
                                    <Condition Property="IsFocused" Value="True" />
                                </MultiTrigger.Conditions>
                                <MultiTrigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="Message" Storyboard.TargetProperty="Opacity" To="0" Duration="00:00:00.2" />
                                            <ColorAnimation Storyboard.TargetName="BackgroundBorder" Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)" To="#FFFFFF" Duration="00:00:00.2" />
                                        </Storyboard>
                                    </BeginStoryboard>
                                </MultiTrigger.EnterActions>
                                <MultiTrigger.ExitActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="Message" Storyboard.TargetProperty="Opacity" Duration="00:00:00.2" />
                                            <ColorAnimation Storyboard.TargetName="BackgroundBorder" Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)" Duration="00:00:00.2" />
                                        </Storyboard>
                                    </BeginStoryboard>
                                </MultiTrigger.ExitActions>
                            </MultiTrigger>
                            <!--<Trigger Property="HasText" Value="True">
                            <Trigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="Message" Storyboard.TargetProperty="Opacity" To="0" Duration="00:00:00.2" />
                                        <ColorAnimation Storyboard.TargetName="BackgroundBorder" Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)" To="#FFFFFF" Duration="00:00:00.2" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.EnterActions>
                            <Trigger.ExitActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="Message" Storyboard.TargetProperty="Opacity" Duration="00:00:00.2" />
                                        <ColorAnimation Storyboard.TargetName="BackgroundBorder" Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)" Duration="00:00:00.2" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.ExitActions>
                        </Trigger>-->

                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    
    <Grid Background="{Binding ElementName=This, Path=Background}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBox x:Name="m_textBox" Margin="10" Height="30" Style="{StaticResource DarkTextBox}" Padding="7,3,150,-3" Text="{Binding Path=TaskString, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}" GotFocus="m_textBox_GotFocus" TextChanged="m_textBox_TextChanged" PreviewMouseLeftButtonDown="m_textBox_PreviewMouseLeftButtonDown" tm:Attached.IsSelected="{Binding ElementName=TaskDetailsReveal, Path=IsExpanded}" />

        <!-- Buttons -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" VerticalAlignment="Top" HorizontalAlignment="Right" Margin="0,15,15,0" Visibility="{Binding ElementName=TaskDetailsReveal, Path=IsExpanded, Converter={StaticResource BooleanToVisibilityConverter}}">
            <Button Style="{StaticResource FlatButton}" Width="60" Height="20" Tag="First" Click="Button_Click" Margin="0,0,3,0">Save</Button>
            <Button Style="{StaticResource FlatButton}" Width="60" Height="20" Tag="Last" Click="CancelButton_Click" IsCancel="True" Command="tm:Commands.Cancel" CommandParameter="{Binding Task}">Cancel</Button>
            <!--<Button Style="{StaticResource BlackButtonStyle}" Width="60" FontSize="12" Height="20" Margin="5,0,0,0" >Task Info</Button>-->
            <ToggleButton Style="{StaticResource DefaultToggleButtonStyle }" x:Name="btnTaskInfo" Width="60" Height="20" FontSize="8" Margin="5,0,0,0" HorizontalContentAlignment="Center" VerticalContentAlignment="Top" HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="Light">Task Info</ToggleButton>
            
            <Popup IsOpen="{Binding IsChecked, Mode=TwoWay, ElementName=btnTaskInfo}" StaysOpen="False"  PlacementTarget="{Binding ElementName=btnTaskInfo}" Placement="Bottom" PopupAnimation="Slide" >
                <Border BorderBrush="SlateBlue" BorderThickness="1"  CornerRadius="2" >
                    <tm:TaskInfoView /> <!--id ="{Binding Path=DataContext.SelectedTask.id,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged,FallbackValue=100,RelativeSource={RelativeSource  AncestorType={x:Type Local:WindowElement}}}" 
                                                           task_desc="{Binding Path=DataContext.SelectedTask.task_desc,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged,FallbackValue=100,RelativeSource={RelativeSource  AncestorType={x:Type Local:WindowElement}}}"  ></tm:TaskInfoView>-->
                </Border>
            </Popup>
        </StackPanel>

        <Border x:Name="RevealContainer" Grid.Row="1" Margin="0,0,0,0">
            <!--<Reveal x:Name="TaskDetailsReveal" Duration="150">-->
            <Expander x:Name="TaskDetailsReveal" Grid.Row="1" Margin="0,0,0,0">
                <Grid Width="{Binding ElementName=RevealContainer, Path=ActualWidth}" Margin="0,-4,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="40" />
                        <ColumnDefinition Width="40" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="200" />
                        <ColumnDefinition Width="41" />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="29" />
                        <RowDefinition Height="29" />
                        <RowDefinition Height="8" />
                    </Grid.RowDefinitions>

                    <!-- Divider -->
                    <Rectangle Width="1" HorizontalAlignment="Right" Fill="#40000000" Grid.RowSpan="2" Grid.Column="2" Margin="0,4" />
                    <Rectangle Width="1" HorizontalAlignment="Left" Fill="#30FFFFFF" Grid.RowSpan="2" Grid.Column="3" Margin="0,4" />

                    <!-- Fake CheckBox -->
                    <Border SnapsToDevicePixels="True" Grid.RowSpan="2" Grid.Column="1" Margin="-4,-1,4,0" VerticalAlignment="Center" Width="18" Height="18" Background="#80FFFFFF" BorderBrush="#40000000" BorderThickness="1" />

                    <!-- Description -->
                    <tm:EditableTextBlock AcceptsReturn="False" Grid.Column="2" Margin="-6,-4,12,-4" VerticalAlignment="Bottom" Text="{Binding Path=Task.task_desc}" InfoText="Description of the task" Foreground="White">
                        <tm:EditableTextBlock.Style>
                            <Style TargetType="tm:EditableTextBlock">
                                <Setter Property="SnapsToDevicePixels" Value="True" />
                                <Setter Property="OverridesDefaultStyle" Value="True" />
                                <Setter Property="KeyboardNavigation.TabNavigation" Value="None" />
                                <Setter Property="FocusVisualStyle" Value="{x:Null}" />
                                <Setter Property="FontFamily" Value="{StaticResource PrimaryFontFamily}" />
                                <Setter Property="FontSize" Value="17" />
                                <Setter Property="FontWeight" Value="Light" />
                                <Setter Property="AllowDrop" Value="True" />
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="tm:EditableTextBlock">
                                            <Grid>
                                                <Border Name="Border" Background="#FFFFFF" CornerRadius="2" Opacity="0.0" Margin="1" />

                                                <TextBlock x:Name="InfoTextBlock" Text="{TemplateBinding InfoText}" Margin="8,2,6,4" Opacity="0.65" />
                                                <ScrollViewer x:Name="PART_ContentHost" Margin="6,2,6,4" />
                                                <TextBlock x:Name="InvisibleText" Text="{TemplateBinding Text}" Opacity="0" HorizontalAlignment="Left" IsHitTestVisible="False" />
                                                <Rectangle x:Name="CrossoutRectangle" Height="1" Margin="8,2,4,-2" VerticalAlignment="Center" Width="{Binding ElementName=InvisibleText, Path=ActualWidth}" HorizontalAlignment="Left" Fill="#C02A2F31" Visibility="Collapsed" IsHitTestVisible="False" />
                                            </Grid>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsEnabled" Value="False">
                                                    <Setter TargetName="Border" Property="Background" Value="#EEEEEE" />
                                                    <Setter TargetName="Border" Property="BorderBrush" Value="#EEEEEE" />
                                                    <Setter Property="Foreground" Value="#888888" />
                                                </Trigger>
                                                <Trigger Property="IsInfoTextVisible" Value="False">
                                                    <Setter TargetName="InfoTextBlock" Property="Visibility" Value="Collapsed" />
                                                </Trigger>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="Border" Property="Opacity" Value="0.1" />
                                                </Trigger>
                                                <Trigger Property="IsFocused" Value="True">
                                                    <Setter TargetName="InfoTextBlock" Property="Visibility" Value="Collapsed" />
                                                    <Setter TargetName="Border" Property="Opacity" Value="0.2" />
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </tm:EditableTextBlock.Style>
                    </tm:EditableTextBlock>

                    <!-- end_date Date -->
                    <StackPanel Orientation="Horizontal" Grid.Column="2" Grid.Row="1" Margin="2,0,0,0" VerticalAlignment="Top" Opacity="{Binding Task.IsComplete, Converter={StaticResource DimCompleteItemConverter}}">
                        <TextBlock Foreground="White" FontSize="14" Text="by" VerticalAlignment="Center" />
                        <tm:DateDropDown Date="{Binding Task.end_date, Mode=TwoWay}" FontSize="14" Margin="-4,0,0,0" FontWeight="Bold" Opacity="{Binding Task.completed, Converter={StaticResource HalfDimCompleteItemConverter}}" Foreground="White" />
                    </StackPanel>

                    <!-- Folder Name -->
                    <StackPanel Orientation="Horizontal" Grid.Column="3" Grid.Row="0" Margin="24,2,24,-2" VerticalAlignment="Bottom" Opacity="{Binding Task.IsComplete, Converter={StaticResource DimCompleteItemConverter}}">
                        <TextBlock Foreground="White" FontSize="14" Text="in" VerticalAlignment="Center" />
                        <tm:FolderDropDown Foreground="White" FontSize="14" FontWeight="Bold" Margin="-4,0,0,0" RequestFolderChange="FolderDropDown_RequestFolderChange" />
                    </StackPanel>

                    <!-- Time to Complete -->
                    <StackPanel Orientation="Horizontal" Grid.Column="3" Grid.Row="1" Margin="24,0,24,0" VerticalAlignment="Top" Opacity="{Binding Task.completed, Converter={StaticResource DimCompleteItemConverter}}">
                        <TextBlock FontSize="14" Foreground="White" Text="for" VerticalAlignment="Center" />
                        <tm:TimeSpanDropDown FontSize="14" Duration="{Binding Task.est_time, Mode=TwoWay}" Margin="-4,0,0,0" FontWeight="Bold" Foreground="White" />
                        <!--<tm:TimeSpanDropDown FontSize="14" Duration="{Binding Task.Estimate, Mode=TwoWay}" Margin="-4,0,0,0" FontWeight="Bold" Foreground="White" />-->
                    </StackPanel>

                    <!--<StackPanel Orientation="Horizontal" Grid.Column="3" Grid.Row="1" Margin="24,0,24,0" VerticalAlignment="Top" Opacity="{Binding Task.IsComplete, Converter={StaticResource DimCompleteItemConverter}}">
                        <TextBlock FontSize="14" Foreground="White" Text="for" VerticalAlignment="Center" />
                        <u:TimeSpanDropDown FontSize="14" Duration="{Binding Task.Estimate, Mode=TwoWay}" Margin="-4,0,0,0" FontWeight="Bold" Foreground="White" />
                    </StackPanel>-->

                </Grid>
            </Expander>
            <!--</Reveal>-->
        </Border>

        <Border Background="#6000" VerticalAlignment="Bottom" Height="1" Grid.RowSpan="2" />

    </Grid>
</UserControl>
