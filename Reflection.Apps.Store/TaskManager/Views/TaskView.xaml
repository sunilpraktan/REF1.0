<UserControl x:Class="Reflection.Apps.Store.TaskManager.TaskView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:tm="clr-namespace:Reflection.Apps.Store.TaskManager" mc:Ignorable="d" >

    <Grid>
        <Grid.Resources>
            <tm:BoolToNullableDateTimeConverter x:Key="BoolToNullableDateTimeConverter1"/>
            <tm:PrettyDateConverter x:Key="PrettyDateConverter" ShowOverdue="False" />
            <tm:BoolToDoubleConverter x:Key="DimCompleteItemConverter" TrueValue="0.25" FalseValue="1.0" />
            <tm:ReverseBooleanToVisibilityConverter x:Key="ReverseBooleanToVisibilityConverter" />
            <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
        </Grid.Resources>

        <Grid x:Name="LayoutRoot" MinHeight="58">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="40" />
                <ColumnDefinition Width="40" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="200" />
                <ColumnDefinition Width="41" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Border Grid.Row="1" Grid.ColumnSpan="5" Height="5" VerticalAlignment="Bottom" Margin="0,0,1,-5" IsHitTestVisible="False" BorderBrush="#60000000" BorderThickness="0,1,0,0">
                <Border.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                        <GradientStop Offset="0" Color="#18000000" />
                        <GradientStop Offset="1" Color="#0000" />
                    </LinearGradientBrush>
                </Border.Background>
            </Border>

            <!-- Background -->
            <Border Grid.ColumnSpan="3" Grid.RowSpan="2" Background="#EBEFF0" />
            <Border Grid.Column="3" Grid.ColumnSpan="2" Grid.RowSpan="2" Background="#D6DCDE" />

            <!-- FolderColor -->
            <Border Width="5" Grid.Column="0" Grid.RowSpan="2" HorizontalAlignment="Left" Background="{Binding Path=Task.Folder.Color}" />

            <!-- Edges -->
            <Border Grid.ColumnSpan="3" Grid.RowSpan="100" BorderThickness="0,1" Margin="0,0,0,0" HorizontalAlignment="Left" Width="5">
                <Border.BorderBrush>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                        <GradientStop Offset="0" Color="#40FFFFFF" />
                        <GradientStop Offset="1" Color="#20000000" />
                    </LinearGradientBrush>
                </Border.BorderBrush>
            </Border>
            <Border Grid.ColumnSpan="3" Grid.RowSpan="100" BorderThickness="0,1" Margin="5,0,0,0">
                <Border.BorderBrush>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                        <GradientStop Offset="0" Color="#FFFFFFFF" />
                        <GradientStop Offset="1" Color="#20000000" />
                    </LinearGradientBrush>
                </Border.BorderBrush>
            </Border>
            <Border Grid.Column="3" Grid.ColumnSpan="2" Grid.RowSpan="100" BorderThickness="0,1">
                <Border.BorderBrush>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                        <GradientStop Offset="0" Color="#80FFFFFF" />
                        <GradientStop Offset="1" Color="#20000000" />
                    </LinearGradientBrush>
                </Border.BorderBrush>
            </Border>

            <!-- Delete Button -->
            <Button Style="{StaticResource OpacityButton}" VerticalAlignment="Center" Click="Button_Click" HorizontalAlignment="Right" Grid.Column="2" Grid.RowSpan="2" Width="40" Height="40" IsEnabled="{Binding IsMouseOver, ElementName=LayoutRoot}" ToolTip="Delete This Task">
                <Border VerticalAlignment="Center" HorizontalAlignment="Center">
                    <tm:PixelAlignedImage Source="{StaticResource DeleteXImageSource}" />
                </Border>
            </Button>

            <!-- IsComplete -->

            <!--<CheckBox Name="CompletedCheck" Grid.RowSpan="3" Grid.Column="1" Margin="-4,-1,4,0" VerticalAlignment="Center" Style="{StaticResource CompletedCheckBox}" IsChecked="{Binding Path=Task.completed,Mode=TwoWay, Converter={StaticResource BoolToNullableDateTimeConverter1}}" >-->
            <CheckBox Name="CompletedCheck"  Grid.RowSpan="3" Grid.Column="1" Margin="-4,-1,4,0" VerticalAlignment="Center" Style="{StaticResource CompletedCheckBox}" IsChecked="{Binding Path=Task.completed,Mode=TwoWay, Converter={StaticResource BoolToNullableDateTimeConverter1}}" ToolTip="Completed"  >
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="Checked" >
                        <i:InvokeCommandAction Command="{Binding Path=SelectionChangedCommand_singal1,ElementName=CompletedCheck}" CommandParameter="{Binding SelectedItems,RelativeSource={RelativeSource AncestorType=ListBox}}"/>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
            </CheckBox>
            <!--<CheckBox Grid.RowSpan="3" Grid.Column="1" Margin="-4,-1,4,0" VerticalAlignment="Center" Style="{StaticResource CompletedCheckBox}" IsChecked="{Binding Path=Task.Completed, Converter={x:Static helpers:BoolToNullableDateTimeConverter.Instance }}" ToolTip="Completed" />-->

            <!-- Description -->
            <tm:EditableTextBlock Grid.Column="2" Margin="-6,4,40,-4" VerticalAlignment="Bottom" Text="{Binding Task.task_desc}" TextWrapping="Wrap">
                <tm:EditableTextBlock.Style>
                    <Style TargetType="{x:Type TextBox}">
                        <Setter Property="SnapsToDevicePixels" Value="True" />
                        <Setter Property="OverridesDefaultStyle" Value="True" />
                        <Setter Property="KeyboardNavigation.TabNavigation" Value="None" />
                        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
                        <Setter Property="FontFamily" Value="{StaticResource PrimaryFontFamily}" />
                        <Setter Property="FontSize" Value="17" />
                        <Setter Property="FontWeight" Value="Light" />
                        <Setter Property="AllowDrop" Value="True" />
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="{x:Type TextBox}">
                                    <Border x:Name="Border" Background="#00FFFFFF">
                                        <Grid>
                                            <ScrollViewer x:Name="PART_ContentHost" Margin="6,2,6,4" />
                                            <TextBlock x:Name="InvisibleText" Text="{TemplateBinding Text}" Opacity="0" HorizontalAlignment="Left" IsHitTestVisible="False" />
                                            <Rectangle x:Name="CrossoutRectangle" Height="1" Margin="8,2,4,-2" VerticalAlignment="Center" Width="{Binding ActualWidth, ElementName=InvisibleText}" HorizontalAlignment="Left" Fill="#C02A2F31" Visibility="Collapsed" IsHitTestVisible="False" />
                                        </Grid>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsEnabled" Value="False">
                                            <Setter TargetName="Border" Property="Background" Value="#EEEEEE" />
                                            <Setter TargetName="Border" Property="BorderBrush" Value="#EEEEEE" />
                                            <Setter Property="Foreground" Value="#888888" />
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </tm:EditableTextBlock.Style>
            </tm:EditableTextBlock>

            <!-- Drag Grip -->
            <Border Background="#00FF" Cursor="SizeAll" Grid.RowSpan="2" DataContext="{Binding Task}">
                <Border VerticalAlignment="Center" HorizontalAlignment="Center" Margin="4,0,-4,0">
                    <tm:PixelAlignedImage Source="{StaticResource GripImageSource}" />
                </Border>
            </Border>

            <!-- Due Date -->
            <StackPanel Orientation="Horizontal" Grid.Column="2" Grid.Row="1" Margin="2,0,0,0" VerticalAlignment="Top" Visibility="{Binding Task.completed, Converter={StaticResource ReverseBooleanToVisibilityConverter}}" ToolTip="Due Date">
                <TextBlock Foreground="#D0252b2d" FontSize="14" Text="by" VerticalAlignment="Center" />
                <tm:DateDropDown Date="{Binding Task.end_date, Mode=TwoWay}" FontSize="14" Margin="-4,0,0,0" FontWeight="Bold" ShowOverdue="True" />
            </StackPanel>

            <!-- Completed -->
            <StackPanel Orientation="Horizontal" Grid.Column="2" Grid.Row="1" Margin="2,0,0,0" VerticalAlignment="Top" Visibility="{Binding Task.completed, Converter={StaticResource BooleanToVisibilityConverter}}" ToolTip="Date Completed">
                <TextBlock Foreground="#D0252b2d" FontSize="14" Text="completed" VerticalAlignment="Center" />
                <TextBlock Foreground="#000000" FontSize="14" Text="{Binding Task.completed, Converter={StaticResource PrettyDateConverter}}" VerticalAlignment="Center" FontWeight="Bold" Margin="4,0,0,0" />
            </StackPanel>

            <Grid Grid.RowSpan="2" Grid.Column="3">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <!-- Folder Name -->
                <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="24,2,24,-2" VerticalAlignment="Bottom" Opacity="{Binding Task.IsComplete, Converter={StaticResource DimCompleteItemConverter}}" ToolTip="Folder">
                    <TextBlock Foreground="#D0252b2d" FontSize="14" Text="in" VerticalAlignment="Center" />
                    <tm:FolderDropDown FontSize="14" Margin="-4,0,0,0" FontWeight="Bold" RequestFolderChange="FolderDropDown_RequestFolderChange" />
                </StackPanel>

                <!-- Time to Complete -->
                <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="24,0,24,0" VerticalAlignment="Top" Opacity="{Binding Task.completed, Converter={StaticResource DimCompleteItemConverter}}" ToolTip="Time To Complete">
                    <TextBlock FontSize="14" Foreground="#D0252b2d" Text="for" VerticalAlignment="Center" />
                    <tm:TimeSpanDropDown FontSize="14" Duration="{Binding Task.Estimate, Mode=TwoWay}" Margin="-4,0,0,0" FontWeight="Bold" />
                </StackPanel>

            </Grid>

            <!-- IsImportant -->

            <tm:ReverseThreeStateCheckBox x:Name="PriorityCheck" Grid.Column="5" Grid.RowSpan="100" IsChecked="{Binding Task.tsk_priority}" IsThreeState="True" IsEnabled="{Binding IsMouseOver, ElementName=LayoutRoot}" Cursor="Hand" ToolTip="Priority">
                <tm:ReverseThreeStateCheckBox.Style>
                    <Style TargetType="CheckBox">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="CheckBox">
                                    <Grid Background="#0000">
                                        <Border Name="NullImage" Visibility="Visible" VerticalAlignment="Center" HorizontalAlignment="Center" Width="15" Height="15">
                                            <tm:PixelAlignedImage Source="{DynamicResource checkNull}" />
                                        </Border>
                                        <Border x:Name="TrueImage" Visibility="Collapsed" VerticalAlignment="Center" HorizontalAlignment="Center" Width="15" Height="15">
                                            <tm:PixelAlignedImage Source="{DynamicResource checkTrue}" />
                                        </Border>
                                        <Border x:Name="FalseImage" Visibility="Collapsed" VerticalAlignment="Center" HorizontalAlignment="Center" Width="15" Height="15">
                                            <tm:PixelAlignedImage Source="{DynamicResource checkFalse}" />
                                        </Border>
                                    </Grid>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsEnabled" Value="False">
                                            <Trigger.EnterActions>
                                                <BeginStoryboard>
                                                    <Storyboard>
                                                        <DoubleAnimation Storyboard.TargetName="NullImage" Storyboard.TargetProperty="Opacity" To="0.0" Duration="00:00:00.2" />
                                                    </Storyboard>
                                                </BeginStoryboard>
                                            </Trigger.EnterActions>
                                            <Trigger.ExitActions>
                                                <BeginStoryboard>
                                                    <Storyboard>
                                                        <DoubleAnimation Storyboard.TargetName="NullImage" Storyboard.TargetProperty="Opacity" To="0.5" Duration="00:00:00.1" />
                                                    </Storyboard>
                                                </BeginStoryboard>
                                            </Trigger.ExitActions>
                                        </Trigger>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Trigger.EnterActions>
                                                <BeginStoryboard>
                                                    <Storyboard>
                                                        <DoubleAnimation Storyboard.TargetName="NullImage" Storyboard.TargetProperty="Opacity" To="1.0" Duration="00:00:00.2" />
                                                    </Storyboard>
                                                </BeginStoryboard>
                                            </Trigger.EnterActions>
                                            <Trigger.ExitActions>
                                                <BeginStoryboard>
                                                    <Storyboard>
                                                        <DoubleAnimation Storyboard.TargetName="NullImage" Storyboard.TargetProperty="Opacity" Duration="00:00:00.1" />
                                                    </Storyboard>
                                                </BeginStoryboard>
                                            </Trigger.ExitActions>
                                        </Trigger>
                                        <Trigger Property="IsChecked" Value="True">
                                            <Setter TargetName="TrueImage" Property="Visibility" Value="Visible" />
                                            <Setter TargetName="NullImage" Property="Visibility" Value="Collapsed" />
                                        </Trigger>
                                        <Trigger Property="IsChecked" Value="False">
                                            <Setter TargetName="FalseImage" Property="Visibility" Value="Visible" />
                                            <Setter TargetName="NullImage" Property="Visibility" Value="Collapsed" />
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>

                        </Setter>
                    </Style>
                </tm:ReverseThreeStateCheckBox.Style>
            </tm:ReverseThreeStateCheckBox>
            
            
            
            <!--<tm:ReverseThreeStateCheckBox Grid.Column="5" Grid.RowSpan="100" IsChecked="{Binding Task.tsk_priority}" IsThreeState="True" IsEnabled="{Binding IsMouseOver, ElementName=LayoutRoot}" Cursor="Hand" ToolTip="Priority">
                <tm:ReverseThreeStateCheckBox.Style>
                    <Style TargetType="CheckBox">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="CheckBox">
                                    <Grid Background="#0000">
                                        <Border x:Name="NullImage" Visibility="Visible" VerticalAlignment="Center" HorizontalAlignment="Center" Width="15" Height="15">
                                            <tm:PixelAlignedImage Source="Reflection.Apps.Store\TaskManager\Images\check_null.png" />
                                        </Border>
                                        <Border x:Name="TrueImage" Visibility="Collapsed" VerticalAlignment="Center" HorizontalAlignment="Center" Width="15" Height="15">
                                            <tm:PixelAlignedImage Source="Reflection.Apps.Store\TaskManager\Images\check_true.png" />
                                        </Border>
                                        <Border x:Name="FalseImage" Visibility="Collapsed" VerticalAlignment="Center" HorizontalAlignment="Center" Width="15" Height="15">
                                            <tm:PixelAlignedImage Source="Reflection.Apps.Store\TaskManager\Images\check_false.png" />
                                        </Border>
                                    </Grid>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsEnabled" Value="False">
                                            <Trigger.EnterActions>
                                                <BeginStoryboard>
                                                    <Storyboard>
                                                        <DoubleAnimation Storyboard.TargetName="NullImage" Storyboard.TargetProperty="Opacity" To="0.0" Duration="00:00:00.2" />
                                                    </Storyboard>
                                                </BeginStoryboard>
                                            </Trigger.EnterActions>
                                            <Trigger.ExitActions>
                                                <BeginStoryboard>
                                                    <Storyboard>
                                                        <DoubleAnimation Storyboard.TargetName="NullImage" Storyboard.TargetProperty="Opacity" To="0.5" Duration="00:00:00.1" />
                                                    </Storyboard>
                                                </BeginStoryboard>
                                            </Trigger.ExitActions>
                                        </Trigger>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Trigger.EnterActions>
                                                <BeginStoryboard>
                                                    <Storyboard>
                                                        <DoubleAnimation Storyboard.TargetName="NullImage" Storyboard.TargetProperty="Opacity" To="1.0" Duration="00:00:00.2" />
                                                    </Storyboard>
                                                </BeginStoryboard>
                                            </Trigger.EnterActions>
                                            <Trigger.ExitActions>
                                                <BeginStoryboard>
                                                    <Storyboard>
                                                        <DoubleAnimation Storyboard.TargetName="NullImage" Storyboard.TargetProperty="Opacity" Duration="00:00:00.1" />
                                                    </Storyboard>
                                                </BeginStoryboard>
                                            </Trigger.ExitActions>
                                        </Trigger>
                                        <Trigger Property="IsChecked" Value="True">
                                            <Setter TargetName="TrueImage" Property="Visibility" Value="Visible" />
                                            <Setter TargetName="NullImage" Property="Visibility" Value="Collapsed" />
                                        </Trigger>
                                        <Trigger Property="IsChecked" Value="False">
                                            <Setter TargetName="FalseImage" Property="Visibility" Value="Visible" />
                                            <Setter TargetName="NullImage" Property="Visibility" Value="Collapsed" />
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>

                        </Setter>
                    </Style>
                </tm:ReverseThreeStateCheckBox.Style>
            </tm:ReverseThreeStateCheckBox>-->

        </Grid>

    </Grid>
</UserControl>
