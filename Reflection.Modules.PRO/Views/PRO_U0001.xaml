<!--Purchase Requisition PR Data Collection Popup window. -->
<Local:WindowElement x:Class="Reflection.Modules.PRO.Views.PRO_U0001"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
        xmlns:Local="http://Reflection/schemas/2010/xaml"
        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"
        mc:Ignorable="d"
        Style="{StaticResource DefaultWorkspaceWindowStyleReport}" 
        Title="{x:Static Local:AppSessionState.ViewTitle}" >
    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Loaded">
            <i:InvokeCommandAction Command="{Binding Path=DataContext.cmdWindowLoadEvent,RelativeSource={RelativeSource AncestorType=Local:WindowElement,AncestorLevel=1}}" 
                                                                CommandParameter="{Binding Text, ElementName=txtStatus}" />
        </i:EventTrigger>
    </i:Interaction.Triggers>
    <Grid VerticalAlignment="Top" HorizontalAlignment="Left" Height="500" Width="950">
        <Grid.Resources>
            <Style TargetType="{x:Type TextBlock}">
                <Setter Property="Foreground" Value="Black" />
            </Style>
        </Grid.Resources>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" >
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox Grid.Row="01" Grid.Column="0" Name="SearchBox" Background="Transparent" Text="{Binding FLTR_STR_ITEMS, UpdateSourceTrigger=PropertyChanged}"  />
            <Button Grid.Row="01" Grid.Column="1" Margin="0,0,0,0" Width="80" VerticalAlignment="Bottom" HorizontalAlignment="Right" Height="25" Content="Execute" Style="{StaticResource FlatButton}" Foreground="Black"
                    Command="{Binding Path= cmdReturnPR}" CommandParameter="ReferenceDocument" >
            </Button>
        </Grid>
        <DataGrid x:Name="DataLV" Grid.Row="1" HorizontalAlignment="Left" VerticalAlignment="Top" 
            ItemsSource="{Binding DataGridCollection}" Width="950" Height="470" AutoGenerateColumns="False" IsReadOnly="True" SelectionUnit="FullRow"
            Local:DataGridTextSearch.SearchValue="{Binding ElementName=SearchBox, Path=Text, UpdateSourceTrigger=LostFocus}"                                                                        
            Background="Transparent" RowBackground="Transparent" GridLinesVisibility="Horizontal" BorderThickness="1" SelectionMode="Single" 
            HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto"  Local:DataGridBehavior.DisplayRowNumber="True" >
            <DataGrid.Columns>
                <DataGridTemplateColumn Header="Check" Width="25">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <CheckBox HorizontalAlignment="Center" VerticalAlignment="Center" IsChecked="{Binding Path=selected, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="ITEM CODE" Binding="{Binding item_code}" Width="Auto"  MinWidth="100" />
                <DataGridTextColumn Header="ITEM NAME" Binding="{Binding item_name}" Width="Auto" MinWidth="150" />
                <DataGridTextColumn Header="UNIT" Binding="{Binding unit_code}" Width="Auto"  MinWidth="40"  />
                <DataGridTextColumn Header="QTY" Binding="{Binding qty,StringFormat='#0.00#'}" Width="Auto" MinWidth="30" />
                <DataGridTextColumn Header="REQ DT." Binding="{Binding issue_date, StringFormat={}{0:dd/MM/yyyy}}" />
                <DataGridTextColumn Header="REQUESTER " Binding="{Binding emp_name}" Width="Auto"  />
                <DataGridTextColumn Header="DOC TYPE " Binding="{Binding doc_type_name}" Width="Auto"  />
                <DataGridTextColumn Header="REF DOC " Binding="{Binding ref_doc_no}" Width="Auto"  MinWidth="120" />
                <DataGridTextColumn Header="REF ROW ID" Binding="{Binding ref_item_row_id}" Width="35"  />
                <DataGridTextColumn Header="PARTY " Binding="{Binding party_code}" Width="Auto" />
                <DataGridTextColumn Header="PARTNER " Binding="{Binding party_name}" Width="Auto" MaxWidth="200" />

            </DataGrid.Columns>
        </DataGrid>

    </Grid>
</Local:WindowElement>
