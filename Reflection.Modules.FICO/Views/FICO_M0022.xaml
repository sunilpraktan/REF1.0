<!-- Posting Period -->
<Local:WindowElement x:Class="Reflection.Modules.FICO.Views.FICO_M0022"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:Local="http://Reflection/schemas/2010/xaml"
              
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             mc:Ignorable="d"
             Style="{StaticResource DefaultWorkspaceWindowStyle}"
             Title="{x:Static Local:AppSessionState.ViewTitle}" >
    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Loaded">
            <i:InvokeCommandAction Command="{Binding Path=DataContext.cmdWindowLoadEvent,RelativeSource={RelativeSource AncestorType=Local:WindowElement,AncestorLevel=1}}" 
                                   CommandParameter="{Binding Text, ElementName=txtStatus}" />
        </i:EventTrigger>
    </i:Interaction.Triggers>
    <Grid VerticalAlignment="Top" HorizontalAlignment="Left" Width="800" Height="400">
        <Grid.Resources>
            <Style TargetType="{x:Type TextBlock}">
                <Setter Property="Foreground" Value="Black"/>
            </Style>
        </Grid.Resources>
        <TabControl x:Name="tbcMaster" SelectedIndex="{Binding Path=SelectedTabControlIndex,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}">
            <TabItem Header="Transaction">
                <Grid x:Name="gRoot" VerticalAlignment="Top" HorizontalAlignment="Left" Margin="80,60,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition  Height="Auto"/>
                        <RowDefinition  Height="Auto"/>
                        <RowDefinition  Height="Auto"/>
                        <RowDefinition  Height="Auto"/>
                        <RowDefinition  Height="Auto"/>
                        <RowDefinition  Height="Auto"/>
                        <RowDefinition  Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Post year" FontSize="16" Margin="0,5,0,0"/>
                    <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding MasterEntity.post_year,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"  Margin="0,0,30,0" Width="100" />

                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Final year" FontSize="16" Margin="0,5,0,0"/>
                    <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding MasterEntity.fin_year,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"  Margin="20,0,0,0" Width="150" />

                    <TextBlock Grid.Row="2" Grid.Column="0" Text="Calender year" FontSize="16" Margin="0,5,0,0"/>
                    <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding MasterEntity.calender_year,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"  Margin="0,0,30,0" Width="100" />

                    <TextBlock Grid.Row="3" Grid.Column="0" Text="Post Month" FontSize="16" Margin="0,5,0,0"/>
                    <TextBox Grid.Row="3" Grid.Column="1" Text="{Binding MasterEntity.post_mon,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"  Margin="0,0,40,0" Width="90" />

                    <TextBlock Grid.Row="4" Grid.Column="0" Text="Calender Days" FontSize="16" Margin="0,5,0,0"/>
                    <TextBox Grid.Row="4" Grid.Column="1" Text="{Binding MasterEntity.cal_days,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"  Margin="0,0,40,0" Width="90" />

                    <TextBlock Grid.Row="5" Grid.Column="0" Text="Quarter" FontSize="16" Margin="0,5,0,0"/>
                    <TextBox Grid.Row="5" Grid.Column="1" Text="{Binding MasterEntity.qtr,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"  Margin="0,0,30,0" Width="100" />

                    <TextBlock Grid.Row="0" Grid.Column="2" Text="Posting Period" FontSize="16" Margin="50,5,0,0"/>
                    <TextBox Grid.Row="0" Grid.Column="3" Text="{Binding MasterEntity.posting_period,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"  Margin="0,0,100,0" Width="80" />

                    <TextBlock Grid.Row="1" Grid.Column="2" Text="Shift year" FontSize="16" Margin="50,5,0,0"/>
                    <TextBox Grid.Row="1" Grid.Column="3" Text="{Binding MasterEntity.shift_year,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"  Margin="0,0,100,0" Width="80" />

                    <TextBlock Grid.Row="2" Grid.Column="2" Text="Short Description" FontSize="16" Margin="50,5,0,0"/>
                    <TextBox Grid.Row="2" Grid.Column="3" Text="{Binding MasterEntity.short_desc,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"  Margin="0,0,30,0" Width="150" />

                    <TextBlock Grid.Row="3" Grid.Column="2" Text="Long Description" FontSize="16" Margin="50,5,0,0"/>
                    <TextBox Grid.Row="3" Grid.Column="3" Text="{Binding MasterEntity.long_desc,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"  Margin="20,0,0,0" Width="200" />

                    <TextBlock Grid.Row="4" Grid.Column="2" Text="Active" Margin="50,5,0,0" FontSize="15"/>
                    <CheckBox Grid.Row="4" Grid.Column="3" IsChecked="{Binding MasterEntity.active, Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" Width="130" Margin="0,5,40,0" />
                </Grid>
            </TabItem>
            <TabItem Header="View">
                <Grid HorizontalAlignment="Left" VerticalAlignment="Top">

                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="AUTO"/>
                        <ColumnDefinition Width="AUTO"/>
                        <ColumnDefinition Width="auto"/>
                        <ColumnDefinition Width="auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Row="0">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="auto"/>
                                <ColumnDefinition Width="auto"/>
                                <ColumnDefinition Width="auto"/>
                            </Grid.ColumnDefinitions>

                            <Local:SearchTextBox Grid.Row="0" Grid.Column="0" Width="700" Name="SearchBox" Background="Transparent" Text="{Binding FilterString, UpdateSourceTrigger=PropertyChanged}"/>
                            <Button Grid.Row="0" Grid.Column="1" Height="20" Width="80" Margin="0,0,0,5" Style="{StaticResource NormalButtonStyle}" Command="{Binding ExportCommand}">Export</Button>
                        </Grid>

                        <DataGrid x:Name="DataLV" Grid.Row="2" HorizontalAlignment="Left" VerticalAlignment="Top" ItemsSource="{Binding PO_Collection}" Width="780" Height="380"   AutoGenerateColumns="False" IsReadOnly="True" SelectionUnit="FullRow"
                                Local:DataGridTextSearch.SearchValue="{Binding ElementName=SearchBox, Path=Text, UpdateSourceTrigger=LostFocus}"                                                                        
                                Background="Transparent" RowBackground="Transparent" GridLinesVisibility="Horizontal" BorderThickness="1" SelectionMode="Single"  VerticalScrollBarVisibility="Visible" HorizontalScrollBarVisibility="Visible">

                            <DataGrid.Columns>

                                <DataGridTemplateColumn Header="Select" Width="auto">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <Button x:Name="btndocno" Width="20" Height="20"
                                                    Command="{Binding Path=DataContext.CmdLoadDocByDocNumber, RelativeSource={RelativeSource Mode=FindAncestor,AncestorType=DataGrid}}" 
                                                    CommandParameter="{Binding SelectedItems, RelativeSource={RelativeSource Mode=FindAncestor,AncestorType=DataGrid}}"                                                    
                                                    Background="Transparent" BorderBrush="Transparent" HorizontalAlignment="Center" >
                                                <StackPanel Orientation="Horizontal">
                                                    <Image Source="{StaticResource imgArrow_right}"   >
                                                    </Image>
                                                </StackPanel>
                                            </Button>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>

                                <DataGridTextColumn Header="Post Year" Width="auto" Binding="{Binding post_year}"/>
                                <DataGridTextColumn Header="Final Year" Width="auto" Binding="{Binding fin_year}" />
                                <DataGridTextColumn Header="Calender Year" Width="auto" Binding="{Binding calender_year}"/>

                            </DataGrid.Columns>
                        </DataGrid>
                    </StackPanel>
                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</Local:WindowElement>
