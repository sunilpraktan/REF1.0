<UserControl x:Class="Reflection.Presentation.Services.DocumentViewrControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="http://Reflection/schemas/2010/xaml"
             xmlns:services="clr-namespace:Reflection.Presentation.Services"
             mc:Ignorable="d" Height="500" Width="700"
            >
    <UserControl.Resources>
        <SolidColorBrush x:Key="BackgroundBrush" Color="#FF323F56" />
        <Thickness x:Key="BorderThickness">1,1,1,1</Thickness>
        <SolidColorBrush x:Key="BorderBrush" Color="#FFFFFFFF" />
        <SolidColorBrush x:Key="FileInfoBackgroundBrush" Color="#FFD8D8D8" />
        <Style x:Key="BorderStyle" TargetType="{x:Type Border}">
            <Setter Property="BorderThickness" Value="{DynamicResource BorderThickness}" />
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}" />
            <Setter Property="CornerRadius" Value="10,10,10,10" />
        </Style>
        <Style x:Key="HeaderStyle" TargetType="{x:Type TextBlock}">
            <Setter Property="FontFamily" Value="Calibri,Segoe UI" />
            <Setter Property="FontSize" Value="18" />
            <Setter Property="Foreground" Value="{DynamicResource FontColor}" />
            <Setter Property="Margin" Value="10,0,10,10" />
        </Style>
        <!-- LightBrush is used for content areas such as Menu, Tab Control background -->
        <LinearGradientBrush x:Key="LightBrush" EndPoint="0,1" StartPoint="0,0">
            <GradientStop Color="#FFF" Offset="0.0" />
            <GradientStop Color="#EEE" Offset="1.0" />
        </LinearGradientBrush>
        <Style x:Key="SectionHeaderStyle" TargetType="{x:Type TextBlock}">
            <Setter Property="TextWrapping" Value="NoWrap" />
            <Setter Property="TextTrimming" Value="None" />
            <Setter Property="Foreground" Value="{DynamicResource FontColor}" />
            <Setter Property="FontWeight" Value="Bold" />
            <Setter Property="FontFamily" Value="Calibri,Segoe UI" />
            <Setter Property="FontSize" Value="13" />
        </Style>
        <!-- This is the style for the list of tags on the Person Info control -->
        <Style x:Key="TagsListStyle" TargetType="{x:Type ListBox}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ListBox}">
                        <ScrollViewer VerticalScrollBarVisibility="Disabled" HorizontalScrollBarVisibility="Disabled" Margin="5,5,5,5">
                            <WrapPanel IsItemsHost="True" />
                        </ScrollViewer>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <!-- TagsList ItemTemplate-->
        <DataTemplate x:Key="TagsItemTemplate">
            <TextBlock Text="{Binding}" Foreground="{StaticResource FontColor}" />
        </DataTemplate>

        <services:ImageConverter x:Key="ImageConverter"/>

        <!-- Converter for showing/hiding features based on boolean property -->
        <services:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>

        <!-- Converter for showing/hiding features based on boolean property -->
        <services:NotBoolToVisibilityConverter x:Key="NotBoolToVisibilityConverter"/>

        <!-- Converter for disabling -->
        <services:NotConverter x:Key="NotConverter"/>

        <Style x:Key="formatTextStyle" TargetType="{x:Type ButtonBase}">
            <Setter Property="FontFamily" Value="Palatino Linotype"/>
            <Setter Property="Width" Value="30"/>
            <Setter Property="FontSize" Value ="14"/>
        </Style>

        <Style x:Key="formatImageStyle" TargetType="{x:Type ButtonBase}">
            <Setter Property="Width" Value="30" />
            <Setter Property="CommandTarget" Value="{Binding ElementName=StoryRichTextBox}"/>
        </Style>
        <!-- This is the style for the avatar textblock text -->
        <Style x:Key="AvatarTextBlockStyle" TargetType="{x:Type TextBlock}">
            <Setter Property="Foreground" Value="{DynamicResource FontColor}" />
        </Style>
        <SolidColorBrush x:Key="FlowDocumentFontColor" Color="#FF1F3066" />
        <!-- This is the style for the list of photos on the PersonInfo control -->
        <Style x:Key="PhotosListStyle" TargetType="{x:Type ListBox}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ListBox}">
                        <ScrollViewer VerticalScrollBarVisibility="Disabled" HorizontalScrollBarVisibility="Disabled" Margin="5,5,5,5">
                            <WrapPanel IsItemsHost="True" VerticalAlignment="Top" />
                        </ScrollViewer>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- This is the style for the photo containers in the list of photos on the PersonInfo control -->
        <Style x:Key="PhotosListItemStyle" TargetType="{x:Type ListBoxItem}">
            <Setter Property="Margin" Value="0,0,10,10" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ListBoxItem}">
                        <Grid SnapsToDevicePixels="true">
                            <Border x:Name="Border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" />
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
                        </Grid>
                        <ControlTemplate.Triggers>
                            <!-- Change IsSelected background to set the selection color for the photos -->
                            <Trigger Property="IsSelected" Value="true">
                                <Setter Property="Background" Value="#FFFFFF" TargetName="Border" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    <Border Margin="10,0,10,10" Style="{DynamicResource BorderStyle}" Background="{DynamicResource BackgroundBrush}">
        <DockPanel>

            <!-- Header Panel-->
            <DockPanel Margin="0,0,10,0" DockPanel.Dock="Top" Height="30">
                <!-- Star -->
                <Path StrokeThickness="1.500000" Stroke="#ffffffff" StrokeMiterLimit="1.000000" Fill="#ff3952a3" Data="F1 M 9.816406,1.694336 L 12.351074,6.830078 L 18.018066,7.654297 L 13.918457,11.654297 L 14.886230,17.299805 L 9.816406,14.630859 L 4.746094,17.299805 L 5.713867,11.654297 L 1.611328,7.654297 L 7.281250,6.830078 L 9.816406,1.694336 Z" HorizontalAlignment="Left" Margin="10,0,0,0" VerticalAlignment="Center"/>
                <TextBlock TextTrimming="CharacterEllipsis" MaxWidth="700" MaxHeight="30"  LineStackingStrategy="MaxHeight" Text="{Binding Path=Name, Mode=Default, FallbackValue=Document Viewer}" Style="{DynamicResource HeaderStyle}" FontWeight="Bold" Margin="5,0,0,0" VerticalAlignment="Center" Foreground="{DynamicResource HeaderFontColor}"/>
                <Button Margin="0,0,3,0" Width="Auto" MinWidth="40" Content="Back" Grid.Column="1" HorizontalAlignment="Right" VerticalAlignment="Center" DockPanel.Dock="Right" Height="15" x:Name="CloseButton" IsCancel="True" IsDefault="True" FontSize="10" Style="{StaticResource RedButtonStyle}"/>
            </DockPanel>

            <Border CornerRadius="0,0,10,10" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,1,0,0" Background="{DynamicResource FileInfoBackgroundBrush}">
                <Grid DockPanel.Dock="Top">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="0"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Photos Info-->

                    <Border Margin="10,10,5,5" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1,1,1,1" CornerRadius="10,10,0,0">
                        <StackPanel>
                            <Menu VerticalAlignment="Bottom" Margin="10,5,0,0" Style="{DynamicResource MenuStyle}">
                                <MenuItem Header="Add" Style="{DynamicResource MenuItemStyle}">
                                    <MenuItem x:Name="AddDocument" Header="AddNewImage" Click="AddDocument_Click" IsCheckable="False" IsChecked="False" Icon="{StaticResource openico}"/>
                                    <MenuItem Header="LinkExistingImage" IsCheckable="False" IsChecked="False"/>
                                </MenuItem>
                                <MenuItem Header="Edit" Style="{DynamicResource MenuItemStyle}" Visibility="{Binding Path=HasPhoto, Converter={StaticResource BoolToVisibilityConverter}, Mode=Default}">
                                    <MenuItem Visibility="{Binding Path=IsLocked, Converter={StaticResource NotBoolToVisibilityConverter}, Mode=Default}" x:Name="Primary" Header="SetAsPrimary"  IsCheckable="False" IsChecked="False" Icon="{StaticResource primaryico}"/>
                                    <MenuItem Visibility="{Binding Path=IsLocked, Converter={StaticResource NotBoolToVisibilityConverter}, Mode=Default}" x:Name="NoPrimary" Header="NoPrimary"  IsCheckable="False" IsChecked="False"/>
                                    <MenuItem Visibility="{Binding Path=IsLocked, Converter={StaticResource NotBoolToVisibilityConverter}, Mode=Default}" x:Name="Delete" Header="Delete"  IsCheckable="False" IsChecked="False" Icon="{StaticResource deleteico}"/>
                                </MenuItem>
                                <MenuItem Header="View" Style="{DynamicResource MenuItemStyle}" Visibility="{Binding Path=HasPhoto, Converter={StaticResource BoolToVisibilityConverter}, Mode=Default}"/>
                            </Menu>

                            <Image x:Name="DisplayPhoto" Margin="10,10,10,10" Stretch="Uniform" MaxHeight="280" Height="280"/>

                            <TextBlock Visibility="{Binding Path=HasPhoto, Converter={StaticResource BoolToVisibilityConverter}, Mode=Default}" HorizontalAlignment="Left" Height="15" MinWidth="100" MaxWidth="415" MaxHeight="20" x:Name="CaptionTextBlock" Margin="10,0,0,0" Foreground="{DynamicResource FontColor}" FontSize="11" FontStyle="Italic" TextTrimming="CharacterEllipsis"/>

                            <StackPanel Orientation="Horizontal" x:Name="TagsStackPanel">
                                <Label Content="TagsColon" FontSize="11" VerticalAlignment="Stretch" Foreground="{DynamicResource FontColor}" HorizontalAlignment="Stretch" Margin="5,0,0,0"/>
                                <ListBox MaxWidth="300" MaxHeight ="25" Height="25" x:Name="TagsListBox" Background="{x:Null}" BorderBrush="{x:Null}" Style="{DynamicResource TagsListStyle}" ItemTemplate="{DynamicResource TagsItemTemplate}" FontSize="11" Margin="0,3,0,0"  />
                            </StackPanel>
                        </StackPanel>
                    </Border>

                    <!-- Photos List Box-->

                    <Border Grid.Row="1" Margin="10,0,5,10" CornerRadius="0,0,10,10" BorderThickness="1,1,1,1" Padding="10,5,10,5" BorderBrush="{DynamicResource BorderBrush}">
                        <Grid>

                            <TextBlock  Visibility="{Binding Path=HasPhoto, Converter={StaticResource NotBoolToVisibilityConverter}, Mode=Default}" HorizontalAlignment="Left" Foreground="{DynamicResource FontColor}" FontSize="10"><Run Text="DropPhotosIntoThisBox"/></TextBlock>
                            <ScrollViewer HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto">

                                <ListBox AllowDrop="True"  ScrollViewer.CanContentScroll="True" x:Name="DocumentListBox" Background="{x:Null}" ItemsSource="{Binding Path=Photos, Mode=Default}" IsSynchronizedWithCurrentItem="True" Drop="DocumentListBox_Drop" Style="{DynamicResource PhotosListStyle}" ItemContainerStyle="{DynamicResource PhotosListItemStyle}" SelectionChanged="DocumentListBox_SelectionChanged" KeyDown="DocumentListBox_KeyDown"  Margin="0,0,0,0"/>
                            </ScrollViewer>
                        </Grid>
                    </Border>

                </Grid>
            </Border>

        </DockPanel>
    </Border>
</UserControl>
