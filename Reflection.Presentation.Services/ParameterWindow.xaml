<UserControl x:Class="Reflection.Presentation.Services.ParameterWindow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Reflection.Presentation.Services" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.Resources>
        <local:DisplayPropertyConverter x:Key="DisplayConverter" />
        <local:ItemsSourceConverter x:Key="SourceConverter" />     
    </UserControl.Resources>
    <Grid>       
        <Grid.RowDefinitions>
            <RowDefinition Height="3*" ></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <!--<ItemsControl Grid.Row="0" Margin="10" ItemsSource="{Binding RelativeSource={RelativeSource AncestorType=Popup,AncestorLevel=1}, Path=DataContext}">
            <ItemsControl.Template>
                <ControlTemplate TargetType="ItemsControl">
                    <Border BorderBrush="Aqua" BorderThickness="1" CornerRadius="15">
                        <ItemsPresenter/>
                    </Border>
                </ControlTemplate>
            </ItemsControl.Template>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    
                    <StackPanel Orientation="Horizontal">                        
                        <TextBlock Width="50" Margin="3,3,3,3" Text="{Binding Path=CatParamName,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"/>
                        <ComboBox x:Name="cboData"  DisplayMemberPath="{Binding Path=CatParamName, Converter={StaticResource DisplayConverter}}" MinWidth="120"
                                     SelectedValue="{Binding Path=SubCatCode,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"
                                    SelectedValuePath="id">
                            <ComboBox.ItemsSource> 
                                <MultiBinding Converter="{StaticResource SourceConverter}" NotifyOnSourceUpdated="True" Mode="OneWay" >
                                    <MultiBinding.Bindings>
                                        <Binding Path="CatParamName" />
                                        <Binding Path="DataContext.MC.MakeList" RelativeSource="{RelativeSource AncestorType={x:Type UserControl}}" />                                        
                                    </MultiBinding.Bindings>
                                </MultiBinding>
                            </ComboBox.ItemsSource>
                        </ComboBox>



                    </StackPanel>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
        <DataGrid  ItemsSource="{Binding RelativeSource={RelativeSource AncestorType=UserControl,AncestorLevel=1}, Path=DataContext.MakeList}" Width="160"/>-->
    </Grid>
</UserControl>
