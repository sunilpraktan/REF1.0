<local:WidgetElement x:Class="Reflection.Presentation.Widgets.CalendarWidgetView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="http://Reflection/schemas/2010/xaml"
             xmlns:res="clr-namespace:Reflection.Presentation.Resources;assembly=Reflection.Presentation.Resources"
             Style="{res:ReflectionResourcesRef ResourceKey=DefaultWidgetStyle}"
             Opacity="0.8">
    <local:WidgetElement.Background>
        <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
            <GradientStop Color="#0f447a" Offset="0"/>
            <GradientStop Color="#afcbe8" Offset="0.587"/>
        </LinearGradientBrush>
    </local:WidgetElement.Background>
    <Grid x:Name="LayoutRoot">
        <Viewbox Stretch="UniformToFill">
            <Calendar x:Name="Calendar"
                      Background="Transparent"
                      BorderThickness="0"
                      SelectionMode="SingleDate" SelectedDate="{Binding SelectedDate}">
            </Calendar>
        </Viewbox>
    </Grid>
</local:WidgetElement>
