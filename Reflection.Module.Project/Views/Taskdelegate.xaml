<Local:WindowElement x:Class="Reflection.Module.Project.Views.Taskdelegate"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:Local="http://Reflection/schemas/2010/xaml"
             xmlns:Res="clr-namespace:Reflection.Presentation.Resources;assembly=Reflection.Presentation.Resources"  
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             mc:Ignorable="d"
             Style="{StaticResource DefaultWorkspaceWindowStyle}"
             Title="Project Task Delegate">
    <Grid VerticalAlignment="Top" HorizontalAlignment="Left" MinHeight="350" MinWidth="700" Margin="0,0,0,0">
        <Grid.Resources>
            <Style TargetType="{x:Type TextBlock}">
                <Setter Property="Foreground" Value="Black" />
            </Style>
        </Grid.Resources>
        <TabControl  SelectedIndex="{Binding Path=SelectedTabControlIndex,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" FontSize="13" Margin="0,0,-6,0">
            <TabItem Header="Transaction" >
                <Grid x:Name="gRoot" VerticalAlignment="Top" HorizontalAlignment="Left" Width="680" Height="310">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"></RowDefinition>
                        <RowDefinition Height="auto"></RowDefinition>
                        <RowDefinition Height="auto"></RowDefinition>
                    </Grid.RowDefinitions>
                    
                    <Grid  Grid.Row="0" VerticalAlignment="Top" HorizontalAlignment="Left">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="auto"></ColumnDefinition>
                            <ColumnDefinition Width="200"></ColumnDefinition>
                            <ColumnDefinition Width="auto"></ColumnDefinition>
                            <ColumnDefinition Width="200"></ColumnDefinition>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="auto"></RowDefinition>
                            <RowDefinition Height="auto"></RowDefinition>
                            <RowDefinition Height="auto"></RowDefinition>
                            <RowDefinition Height="auto"></RowDefinition>
                            <RowDefinition Height="auto"></RowDefinition>
                        </Grid.RowDefinitions>

                        <TextBlock Text="Project Name" VerticalAlignment="Center" Grid.Row="0" Grid.Column="0" Margin="10,0,0,0"></TextBlock>
                        <ComboBox Grid.Row="0" Grid.Column="1" Margin="10,0,0,0" Height="30" Width="140" HorizontalAlignment="Left"></ComboBox>

                        <TextBlock Text="Assigned To" Margin="10,0,0,0" VerticalAlignment="Center" Grid.Row="0" Grid.Column="2" HorizontalAlignment="Right"></TextBlock>
                        <ComboBox Grid.Row="0" Grid.Column="3" Width="140" Height="30" HorizontalAlignment="Left" Margin="10,0,0,0"  ></ComboBox>

                        <TextBlock Text="Delegated Task" FontSize="14" VerticalAlignment="Center" Margin="10,5,0,0" Grid.Row="1" Height="30" Grid.Column="0"></TextBlock>
                        <TextBlock Text="Validation Task" FontSize="14" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="0,0,0,0" Grid.Row="1" Grid.Column="2"></TextBlock>

                        <TextBlock Text="Delegated Title" Margin="10,0,0,0" VerticalAlignment="Center" Grid.Row="2" Grid.Column="0" Height="30" ></TextBlock>
                        <TextBox Grid.Column="1" Grid.Row="2" Margin="10,0,0,0" Width="140" HorizontalAlignment="Left" ></TextBox>
                        
                        <TextBlock Text="Validation Task Title" Margin="0,0,0,0" Grid.Row="2" Width="auto" HorizontalAlignment="Right" Grid.Column="2" VerticalAlignment="Center"></TextBlock>
                        <TextBox Grid.Row="2" Grid.Column="3" Width="140" Height="30" HorizontalAlignment="Left" Margin="10,0,0,0" ></TextBox>

                        <TextBlock Text="Planned Hours" Grid.Row="3" Grid.Column="0" Margin="10,0,0,0" Height="30" VerticalAlignment="Center"></TextBlock>
                        <TextBox Grid.Row="3" Grid.Column="1" Margin="10,0,0,0" Width="140" HorizontalAlignment="Left" ></TextBox>
                        
                        <TextBlock Text="Hours To Validate" VerticalAlignment="Center" Grid.Row="3" Grid.Column="2" Margin="10,0,0,0" HorizontalAlignment="Right"></TextBlock>
                        <TextBox Grid.Row="3" Grid.Column="3" Width="140" Height="30" HorizontalAlignment="Left" Margin="10,0,0,0"></TextBox>

                        <TextBlock Text="Validation Status" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="0,0,0,0" Grid.Row="4" Grid.Column="2" ></TextBlock>
                        <ComboBox Grid.Row="4" Grid.Column="3" Width="140" Height="25" HorizontalAlignment="Left" Margin="10,0,0,0"></ComboBox>
                    </Grid>

                    <Grid Grid.Row="1" >
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"></RowDefinition>
                            <RowDefinition Height="*"></RowDefinition>
                        </Grid.RowDefinitions>

                        <TextBlock Text="New Task Description" FontSize="16" Grid.Row="0" Margin="10,0,0,0"></TextBlock>
                        <TextBox Grid.Row="1" Height="80" Margin="0,0,150,0"></TextBox>
                    </Grid>

                    <Button Grid.Row="2" FontSize="14" FontWeight="Bold" Width="80" Height="30" Margin="0,10,0,0" HorizontalAlignment="Left" Style="{StaticResource NormalButtonStyle}"  
                                    Content="Delegate" Background="Transparent" />

                    <Grid>
             </Grid>
      </Grid>
            </TabItem>
            <TabItem Header="View" >
                <Grid HorizontalAlignment="Left" VerticalAlignment="Top">
                    <Grid.RowDefinitions>
                        <RowDefinition/>
                        <RowDefinition/>
                        <RowDefinition/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="19*"/>
                        <ColumnDefinition Width="431*"/>
                        <ColumnDefinition Width="0*"/>
                    </Grid.ColumnDefinitions>
                    <Grid Grid.Row="0" Grid.ColumnSpan="2" >
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <Local:SearchTextBox Grid.Column="0" Name="SearchBox" Background="Transparent" Text="{Binding FilterString_FlipGrid, UpdateSourceTrigger=PropertyChanged}"  />
                        <Button Grid.Row="0" Grid.Column="1" Height="20" Margin="4,0,0,0" Width="100" Style="{StaticResource NormalButtonStyle}" Command="{Binding ExportCommand}">Export</Button>
                    </Grid>
                    <DataGrid x:Name="DataLV" Grid.Row="1" HorizontalAlignment="Left" VerticalAlignment="Top" ItemsSource="{Binding FlipDataGridCollection}" Width="690" Height="310" AutoGenerateColumns="False" IsReadOnly="True" SelectionUnit="FullRow"
                                Local:DataGridTextSearch.SearchValue="{Binding ElementName=SearchBox, Path=Text, UpdateSourceTrigger=LostFocus}"                                                                        
                                Background="Transparent" RowBackground="Transparent" GridLinesVisibility="All" BorderThickness="1" SelectionMode="Single"  VerticalScrollBarVisibility="Visible"  HorizontalScrollBarVisibility="Visible" Grid.ColumnSpan="2" >
                        <DataGrid.Columns>

                            <DataGridTemplateColumn Header="Select">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <ToggleButton x:Name="btnlabtrs"
                                                      Command="{Binding Path=DataContext.CmdLoadDocumentByDocumentNumber, RelativeSource={RelativeSource Mode=FindAncestor,AncestorType=DataGrid}}" 
                                                        CommandParameter="{Binding SelectedItems, RelativeSource={RelativeSource Mode=FindAncestor,AncestorType=DataGrid}}"                                                    
                                                         Style="{StaticResource CircleToggleButtonStyle}" Width="25" Height="25" Content="{Binding SetQty,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" HorizontalAlignment="Center" >
                                            <i:Interaction.Triggers>
                                                <i:EventTrigger EventName="Click">
                                                    <i:InvokeCommandAction Command="Local:ContentControl3D.RotateCommand"  />
                                                </i:EventTrigger>
                                            </i:Interaction.Triggers>

                                        </ToggleButton>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <DataGridTextColumn Header="Document No."  Binding="{Binding doc_no}" IsReadOnly="True"/>
                            <DataGridTextColumn Header="Report Type"  Binding="{Binding report_type}" IsReadOnly="True"/>
                            <DataGridTextColumn Header="Document Date"  Binding="{Binding doc_date , StringFormat={}{0:MM/dd/yyyy}}" IsReadOnly="True"/>
                            <DataGridTextColumn Header="Production Date" Binding="{Binding prod_date, StringFormat={}{0:MM/dd/yyyy}}" IsReadOnly="True"/>
                            <DataGridTextColumn Header="Status" Binding="{Binding t_status}" IsReadOnly="True"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>
        </TabControl>
    </Grid>

</Local:WindowElement>
